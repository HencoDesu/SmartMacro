# План реализации макета SmartMacro UI

Источник: `docs/design/smartmacro-ui.dc.html` (проект Claude Design `6f49190f`), разложен по вариантам в `opt-1*.html`. Токены визуального языка — `nocturne-tokens.md`.

## Что выбрано

| Вариант | Что это | Статус |
|---|---|---|
| **1b** | Главное окно: одно окно, слева режимы (Окна / Макросы / Прогоны / Шаблоны / Лог) + сводка по тегам, справа рабочая область, полоса прогонов всегда внизу | **выбрано автором макета** |
| **1d** | Редактор — canvas с живым прогоном: библиотека слева, граф в центре, лог прогона снизу, инспектор справа | основной режим правки |
| **1e** | Правка ноды на месте (двойной клик разворачивает бокс в редактор самого себя) | второй способ, вместе с 1d |
| **1f** | Ловушка хоткея: пусто → ловит → захвачено (кейкапы) → **конфликт** | виджет |
| **1g** | Бейдж фильтра целей: «8 окон · кроме Склад», разворот показывает поимённо | виджет |
| 1a | Главное окно отдельным окном (как сейчас) | не выбран |

Ключевое следствие 1b: **редактор перестаёт быть модальным диалогом**. Сейчас это `MacrosDialog`; станет режимом в одном окне.

## Чего в движке нет (макет опережает реализацию)

Это не UI-работа. Порядок — снизу вверх: без движка эти части макета нарисовать нечем.

| Из макета | Что требуется в движке |
|---|---|
| ~~⏸ Пауза · ⤼ Шаг · ▷ До курсора · ■ Стоп, «3 / 11»~~ | ✅ D5. Ворота между нодами в `MacroExecutor` (`IMacroDebugger`), команды по IPC на **обход**; ■ отменяет прогон целиком и подписывает это числом обходов |
| ~~Красная точка брейкпоинта на углу ноды, чекбокс «Брейкпоинт на этой ноде»~~ | ✅ D5. **В сессии демона**, не в графе — обоснование в `MacroDebugSession`. Точка стала полосой по левому краю внутри бокса: угловая срезалась. (Не в жёлобе — жёлоб это место между рядами, где идут рёбра) |
| ~~Лог прогона снизу: `0:01.2 · click-server-select · ок · PostMessage 1192,1805 · 40 мс`~~ | ✅ D3b |
| ~~Пройденное приглушено с галочкой и временем, текущая пульсирует~~ | ✅ D5 (русло — D3b). Пульс — отдельное кольцо внутри бокса: анимировать сам `Border` значило бы гасить вместе с ним текст. Идёт **только** пока селектор `.pulsing` активен, поэтому простаивающая канва не держит ни одного таймера |
| ~~Панель «переменные макроса»~~ | ✅ D5. `MacroVariableAnalysis` в `Contracts` — чистая функция графа, тестируется без UI. Live-значение приходит своим видом события `VariableSet`, а не разбором детали `NodeExited` |
| ~~Бейдж «8 окон · кроме Склад», полоски доли, ноль попаданий как ошибка~~ | ✅ D4. ~~новый IPC-запрос~~ — **эта строка была неверна.** `SelectorEvaluator` — чистая логика по тегам, а панель уже держит живой снимок окон с тегами (`WindowDto` из `GetWindows`, обновляемый по `WindowTagsChanged`). Считается на месте. Важно только не переписывать правило второй раз: бейдж, расходящийся с тем, куда реально пойдёт прогон, — худший из возможных здесь дефектов, ради этого правило и переехало в `Contracts` (`TargetSelector.Matches`) |
| ~~«уже занят pw-immunity» в ловушке хоткея~~ | ✅ D4, целиком в панели. Второй вид конфликта — отказ `RegisterHotKey` — панель узнать не может, для него добавлен `GetHotkeyFailures` |
| Группы библиотеки: `pw · 6`, `баг госта · 5`, `прочее · 11` | Группировка по префиксу имени (чисто UI, но правило надо зафиксировать) |
| Режимы «Шаблоны 31» и «Лог» | Браузер шаблонов (`templates/`) и просмотр журнала — новых сущностей нет, но экраны новые |
| ~~«Авто-раскладка»~~ | ✅ D3a |

## Волны

Выполнять строго после стадии 3 (App должен уже говорить по IPC — иначе всё это пишется дважды).

### D1. Токены Nocturne в Avalonia — ✅ `badcadd`
`ResourceDictionary` с рампами, отступами, радиусами, тенями + `ControlTheme` для Button / TextBox / ComboBox / CheckBox по правилам системы (обводка вместо заливки, состояния из рампы, фокус — 2px акцент). Inter в ресурсы приложения. Проверка: существующие экраны перекрашиваются без правки разметки.

Три дефекта нашлись только глазами, сборка и тесты их пропускали: классы `.accent`/`.danger` стояли после `:disabled` и при равной специфичности побеждали порядком — отключённые кнопки рисовались активными; кольцо фокуса не рисовалось вообще, потому что `AdornerLayer.IsClipEnabled` по умолчанию `true` и срезает ровно тот отступ 2px, которым кольцо живёт; `▶` U+25B6 приезжал белым эмодзи мимо `Foreground` (emoji-презентация тянет Segoe UI Emoji вопреки заданному шрифту) — заменён на U+25B8.

Ссылки между словарями — только `{DynamicResource}`: `{StaticResource}` через границу словаря здесь ненадёжен.

### D2. Оболочка 1b — ✅ `34d6dac`
Одно окно: сайдбар режимов со счётчиками, сводка тегов внизу сайдбара, полоса прогонов внизу всегда. Режим «Окна» — плотные строки 32px в колонках `160 / 88 / 1fr`, неопознанные отдельной группой внизу с приглушением и активным полем тега. `MacrosDialog` растворяется в режиме «Макросы». Трей-иконка и hide-to-tray уже удалены стадией 3.

Своя полоса заголовка 30px через `ExtendClientAreaToDecorationsHint` — системная всё равно рисуется светлой вопреки `RequestedThemeVariant`. Виды не подменяются в `ContentControl`, а переключаются видимостью: недописанный тег и грязное состояние редактора переживают смену режима.

Приостановка хоткеев привязана к режиму «Макросы», а не к «взведён пикер»: взвод происходит по клику, а suspend — round trip, первое нажатие успело бы проскочить. При закрытии окна resume дожидается ответа — **демон не перерегистрирует хоткеи сам при обрыве клиента.**

«Шаблоны» и «Лог» отрисованы с честной пустотой и пустым счётчиком — данных за ними нет. `templates/` по IPC не перечисляется и не читается; журнал демона через трубу не ходит вовсе. Обоим нужны новые типы сообщений, и журналу — то же событийное русло, что и D5.

### D3. Canvas (1d + 1e) — разбита надвое
Боксы нод «строками с переносом», рёбра в жёлобах между рядами, у условной ноды оба исхода — подписанные строки внизу бокса (важно: «в конец» не создаёт лишний узел). Пан/зум, перетаскивание, перекоммутация. Инспектор справа (1d) и разворот бокса на месте (1e) — оба, одиночный клик наполняет инспектор, двойной разворачивает. Выпил rows-редактора.

- **D3a** — ✅ `e8897ae`. Canvas целиком, только `SmartMacro.App`. Строковый редактор удалён.
- **D3b** — ✅ `f28f5b7`. Событийное русло прогона: нода, исход, деталь, длительность по IPC.

Группировка библиотеки (решение на запись): префикс — текст до первого дефиса, группой становится префикс, общий **двум и более** макросам, остальное — «прочее». Правило «всё без дефиса — в прочее» отвергнуто: в самом макете «Баг госта · 5» содержит и `Баг госта-Лучник`, и просто `Баг госта`.

Единица прогона — **обход**, не прогон: `RunMacroNode` с селектором форкает по обходу на окно, и все они делят один `RunId`. Чип в макете с `0x140804` — дескриптор окна, а не номер прогона. Отсюда переключатель обходов в полосе лога: иначе веер на десять окон зажёг бы десять боксов одного графа.

Три меры против затопления — по подписке, склейка в пачки не чаще 50 мс, ограниченная очередь со счётом потерь — описаны в `CLAUDE.md`. Коротко: очередь соединения в 256 отключает неуспевающего клиента, а веер даёт сотни событий за сотни миллисекунд, так что без склейки панель отваливалась бы ровно в тот момент, ради которого всё это и делается.

Окно выросло 900×482 → 1520×840: на 900 рельс, библиотека и инспектор оставляли графу меньше 200px.

### D4. Виджеты 1f и 1g — ✅
Ловушка хоткея с кейкапами и состоянием конфликта; бейдж целей с живым числом окон и разворотом.

**1g считается в панели, без нового запроса.** Правило отбора переехало из `SelectorEvaluator` (Core) в `TargetSelector.Matches` (Contracts) и берёт теги, а не окно, — так его зовут обе стороны: демон через прежний `SelectorEvaluator` (остался тонкой типизированной обёрткой, его тесты не менялись), панель — напрямую по своему снимку `WindowDto`. Одна реализация, потому что бейдж, расходящийся с тем, куда реально уйдёт прогон, отменяет смысл бейджа.

Два отступления от макета, оба найдены глазами:
- **Ноль попаданий — ошибка, «нет окон» — нет.** При закрытой игре ни один селектор ни во что не попадает; красить красным каждую ноду каждого макроса — верный способ отучить читать этот цвет. Ноль подсвечивается только когда окна есть.
- **На боксе — только число** («7 окон»), теги в тултипе и в инспекторе. Полная строка выдавливает из 210px-шапки название типа ноды, а оно говорит, что нода делает.

**1f — эволюция `KeyBindingPicker`, не замена.** Семантика захвата (Esc/Del, боковые кнопки мыши, игнор голых модификаторов) выверена руками и осталась нетронутой; переписана отрисовка — `ControlTheme` с шаблоном из кейкапов и четыре состояния через псевдоклассы `:unbound` / `:capturing` / `:conflict`.

**Конфликтов два, и локален только один.** «уже занят pw-immunity» панель отвечает сама по библиотеке. Отказ `RegisterHotKey` (сочетание занято другой программой) виден только демону — раньше он уходил в его лог, которого труба не носит, и пользователь получал «хоткей назначен, но ничего не происходит». Добавлен `GetHotkeyFailures`: запрос, а не событие, потому что список меняется только при перерегистрации, а её вызывает сама панель — она перечитывает его на `Connected`, на `MacrosChanged` и после ответа `ResumeHotkeys`. Пока режим «Макросы» открыт, хоткеи сняты, поэтому только что назначенное сочетание проверяется на выходе из режима; отметка ⚠ на строке библиотеки показывает отказ и для макросов, которые никто не открывал.

Ещё три дефекта нашлись только глазами: `⚠` U+26A0 и `⌨` U+2328 приезжают цветными эмодзи мимо `Foreground` (тот же капкан, что D1 нашла на U+25B6) — заменены на U+25B2 и на голый текст; подсказка в `Auto`-колонке съедала кейкапы, и `Ctrl+Shift+F1` рисовался как «Ctrl + Shift + огрызок»; попап бейджа в `Placement="Bottom"` обрезался правым краем окна.

### D5. Отладчик — ✅
Пауза/шаг/до курсора/брейкпоинты в исполнителе, подсветка на canvas, панель переменных со
статическим анализом. Событийное русло и синхронный лог уехали вперёд в D3b.

**Протокол.** Три запроса (`SetBreakpoints`, `GetBreakpoints`, `DebugCommand`) и четыре новых
вида события в существующем `RunEvents` (`Paused`, `BreakpointHit`, `Resumed`,
`VariableSet`) — ровно как закладывала D3b: новые виды, а не новые типы сообщений, поэтому
подписка, склейка и демультиплексор клиента не тронуты. Команды адресуют **обход**, потому
что канва следит за обходом.

**Брейкпоинты — в сессии демона, не в графе.** Обоснование записано в
`MacroDebugSession`. Коротко: брейкпоинт — факт отладочной сессии, а не макроса; в файле он
попал бы в diff и уехал бы к другому пользователю, а сохранение сделало бы красную точку
«грязным состоянием» редактора. Эргономику «мои брейкпоинты на месте» даёт сам раскол:
демон переживает панель. Проверено вживую — панель убита и поднята заново, точка на месте.

**Три опасности, разобранные явно:**

- **Обход на паузе без наблюдателя.** Счётчик подключённых отладчиков — это счётчик
  подписчиков `SubscribeRunEvents`, который сервер уже освобождает на разрыве соединения.
  Последний отцепившийся **снимает с паузы всё** (и брейкпоинты перестают срабатывать, пока
  никто не смотрит). Продолжить, а не прервать: прогон запущен легально, брошенный на
  середине макрос оставит игру в худшем состоянии. Таймаута бездействия намеренно нет —
  единственный оставшийся случай это «панель открыта, пользователь отошёл», и там пауза
  делает ровно то, что должна. Проверено вживую: панель убита с обходом на паузе, в логе
  демона авто-резюме, прогон дошёл до конца, слот single-flight освобождён.
- **Пауза не должна оставить окно разбуженным.** Ворота стоят между нодами, до старта
  часов ноды. Проверено по коду, а не предположено: `ActivateAsync`/`DeactivateAsync` и
  пробуждение на каждый тик vision целиком внутри `IMacroPrimitives`, так что к возврату
  управления в walker ни одно окно не разбужено. Тест `ABreakpointParksTheWalkBeforeTheNodeRuns`
  проверяет это счётчиком вызовов примитивов.
- **Стоп обязан честно сказать, что он останавливает.** ■ отменяет **прогон целиком**, все
  обходы: веер — это N обходов с одним токеном, и никто, нажимая стоп при десяти клиентах,
  не имеет в виду «останови один». Пауза и шаг остались по обходу — ради этого и есть
  переключатель. Асимметрия подписана: кнопка читается «■ Стоп ×3», тултип — «все 3 обхода».

**Отступления от макета, все найдены глазами:**
- **Точка брейкпоинта стала полосой в жёлобе бокса.** Точка на углу, наполовину снаружи,
  срезается: что-то выше контейнера элемента режет по границе бокса (`ClipToBounds="False"`
  на сгенерированном `ContentPresenter` не помог), и у ноды на canvas-x=0 от неё оставался
  красный огрызок. Полоса по левому краю внутри бокса — форма, которой это означает то же
  самое в любой IDE, и она не режется нигде.
- **Идентификатор обхода и причина паузы — только в шапке лога.** В макете они в чипе
  тулбара; две копии стоили 260px и выдавили «Сохранить» за правый край окна — ровно ту
  кнопку, к которой тянешься после отладки. В тулбаре остались счётчик «3 / 11» и часы.
- **Появилось состояние «пауза запрошена».** ⏸ исполняется на границе ноды, а
  `WaitForElement` может держать её минуту; без этой строки кнопка просто гасла и дальше не
  происходило ничего, что неотличимо от сломанной кнопки.
- **Штамп прогона гасит бейдж целей** — они делят один слот в 210px-шапке, и «✓ 2 мс»
  поверх «нет окон» не читалось как ни то ни другое.
- **`⏸`/`⤼`/`▷|` не использованы**: U+23F8 имеет emoji-презентацию и приезжает цветной
  картинкой мимо `Foreground` — тот же капкан, что D1 нашла на U+25B6, а D4 на U+26A0.
  Оставлены проверенные U+25B8 и U+25A0 и голый текст.
- **«читает» без названия поля, когда потребители читают в разные поля.** Строка
  «step-3, step-5 · в точке клика» утверждала, что тег-нода читает точку клика; разбивка
  уехала в тултип.

**Live-значение переменной — своё событие, а не разбор `NodeExited`.** Деталь
`RecognizeTag` выглядит как `"classes → Жрец"`: вытаскивать значение оттуда — это парсить в
панели строку, формат которой принадлежит демону, и всё равно никогда не увидеть `cursor`,
который не пишет ни одна нода. Частота не проблема: пара событий на обход, а не два на ноду.

## Порядок и риск

D1 → D2 → D3 — визуальная часть, каждая волна самодостаточна и заметна. D4 и D5 добавили движку функции; D5 самая крупная и трогает `MacroExecutor`. Его тесты **расширены, не переписаны**: каждый существующий по-прежнему гоняется с `Debugger = null`, что и есть путь неотлаженного прогона. Гейт волны: 454 → 537 тестов.

Отладчик утверждён автором, так что оговорка «остановиться после D3 и посмотреть, нужен ли он в заявленном объёме» снята. Но её половина сохраняется по существу: подсветка текущей ноды и лог отделены от паузы и брейкпоинтов и уезжают вперёд, в D3b. Если пауза/шаг окажутся невостребованными, потеряна будет только D5, а не то, чем пользуются каждый день.

Волны идут строго по одной. Не из осторожности: две параллельные `dotnet build` дерутся за `obj/`, а посторонняя сборка портит замеры памяти.
