<!--
  «Макросы» — the canvas node editor (wave D3a).
  Source of truth: docs/design/opt-1d.html (canvas + inspector) and opt-1e.html (a box
  expanded into an editor of itself). The rows editor this replaces is gone.

  Four columns of the 1d layout, left to right:

    · the LIBRARY, grouped by name prefix. The rule is MacroLibraryGrouping's, not this
      file's; the panel only renders headings it is handed.
    · the CANVAS. Boxes are laid out "rows with wrapping" and edges run in the gutters
      BETWEEN rows — never diagonally across a box. A conditional carries both outcomes as
      labelled rows along its own bottom edge, which is what makes «в конец» readable
      without a terminal node: an unwired outcome says "→ конец" in its own row and draws
      no line at all.
    · the RUN LOG (D3b), fed by the daemon's RunEvents stream. Its header carries the run
      picker — one graph can be walked by ten windows at once, and both the strip and the
      canvas highlight follow exactly one of them.
    · the INSPECTOR. Single click on a box fills it; double click expands the box in place
      (1e) and both edit the same view-model, so they can never disagree.

  Everything is drawn from Themes/Tokens.axaml. No literal colour belongs in this file.
-->
<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="using:System"
             xmlns:vm="using:SmartMacro.App.ViewModels"
             xmlns:canvas="using:SmartMacro.App.ViewModels.Canvas"
             xmlns:nodes="using:SmartMacro.App.ViewModels.Nodes"
             xmlns:conv="using:SmartMacro.App.Converters"
             xmlns:controls="using:SmartMacro.App.Controls"
             x:Class="SmartMacro.App.Views.MacrosView"
             x:DataType="vm:MacroEditorViewModel">

    <UserControl.Styles>
        <!-- The parameter blocks are a wall of "label: field" pairs; naming the recurring
             roles once keeps token references out of every row. -->
        <Style Selector="TextBlock.label">
            <Setter Property="FontSize" Value="{DynamicResource NocturneFontSizeMicro}" />
            <Setter Property="Foreground" Value="{DynamicResource NocturneNeutral600Brush}" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style Selector="TextBlock.hint">
            <Setter Property="FontSize" Value="{DynamicResource NocturneFontSizeMicro}" />
            <Setter Property="Foreground" Value="{DynamicResource NocturneTextFaintBrush}" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style Selector="TextBlock.eyebrow">
            <Setter Property="FontSize" Value="9.5" />
            <Setter Property="LetterSpacing" Value="0.7" />
            <Setter Property="Foreground" Value="{DynamicResource NocturneTextGhostBrush}" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style Selector="TextBox.mono">
            <Setter Property="FontFamily" Value="{DynamicResource NocturneFontFamilyMono}" />
        </Style>
        <Style Selector="TextBox.numeric">
            <Setter Property="FontFamily" Value="{DynamicResource NocturneFontFamilyMono}" />
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="Padding" Value="2.8,2.8" />
        </Style>

        <!-- ============================ library rows ============================ -->
        <Style Selector="Border.libRow">
            <Setter Property="Height" Value="28" />
            <Setter Property="Padding" Value="12,0,8,0" />
            <Setter Property="BorderThickness" Value="2,0,0,0" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Background" Value="Transparent" />
        </Style>
        <Style Selector="Border.libRow:pointerover">
            <Setter Property="Background" Value="{DynamicResource NocturneAccentTint08Brush}" />
        </Style>
        <Style Selector="Border.libRow.current">
            <Setter Property="Background" Value="{DynamicResource NocturneAccentTint12Brush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource NocturneAccentBrush}" />
            <Setter Property="Padding" Value="10,0,8,0" />
        </Style>

        <!-- ============================== node boxes ============================== -->
        <Style Selector="Border.node">
            <Setter Property="Background" Value="{DynamicResource NocturneNodeSurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource NocturneNodeBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="6" />
            <Setter Property="BoxShadow" Value="{DynamicResource NocturneShadowNode}" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style Selector="Border.node:pointerover">
            <Setter Property="BorderBrush" Value="{DynamicResource NocturneNeutral700Brush}" />
        </Style>
        <Style Selector="Border.node.selected">
            <Setter Property="BorderBrush" Value="{DynamicResource NocturneAccentBrush}" />
            <Setter Property="BoxShadow" Value="{DynamicResource NocturneShadowNodeSelected}" />
        </Style>
        <!-- Passed BEFORE running/paused, so a box that is both (a loop back onto a node
             the walk has already crossed) reads as live. Order is the whole rule here:
             equal-specificity selectors in Avalonia are won by the LAST one, which is the
             trap D1 hit with .accent losing to :disabled. -->
        <Style Selector="Border.node.passed">
            <Setter Property="Background" Value="{DynamicResource NocturneNodePassedBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource NocturneNodePassedBorderBrush}" />
            <Setter Property="Opacity" Value="0.72" />
        </Style>
        <Style Selector="Border.node.running">
            <Setter Property="Background" Value="{DynamicResource NocturneNodeRunningBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource NocturneAccentBrush}" />
            <Setter Property="Opacity" Value="1" />
        </Style>
        <!-- Parked: the accent surface of a live node, edged in danger. "Stopped here" must
             never look like "running here" — they are one keypress apart in consequence. -->
        <Style Selector="Border.node.paused">
            <Setter Property="Background" Value="{DynamicResource NocturneNodeRunningBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource NocturneNodePausedBorderBrush}" />
            <Setter Property="Opacity" Value="1" />
        </Style>
        <!-- Ends of a hovered variable link. An outline, not a fill: the box may also be
             passed or live, and this must sit on top of either without hiding it. -->
        <Style Selector="Border.node.varSource, Border.node.varConsumer">
            <Setter Property="BorderBrush" Value="{DynamicResource NocturneVariableBrush}" />
            <Setter Property="Opacity" Value="1" />
        </Style>

        <!-- The breakpoint marker.
             The mockup draws it as a dot hanging half off the box's top-left corner. That
             does not survive here: something above the item container clips at the box's
             bounds — setting ClipToBounds="False" on the generated ContentPresenter changed
             nothing — so a node at canvas x=0 lost the outer half of its dot and rendered a
             red sliver. Rather than ship a marker whose visibility depends on where the node
             happens to sit, it became a gutter bar down the box's inside edge: the shape
             every IDE uses for the same meaning, unclippable at any position or zoom. -->
        <Style Selector="Border.breakpointBar">
            <Setter Property="Width" Value="4" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Stretch" />
            <Setter Property="CornerRadius" Value="5,0,0,5" />
            <Setter Property="Background" Value="{DynamicResource NocturneDangerBrush}" />
        </Style>

        <!-- The mockup's pulsing current node. A separate ring rather than animating the box:
             fading the Border itself would fade its text with it, and the whole point is that
             the live node stays the most readable one. Invisible and INERT until the class is
             on — Avalonia runs a style's animation only while its selector matches, so an
             idle canvas has no timers at all. -->
        <Style Selector="Border.pulseRing">
            <Setter Property="CornerRadius" Value="6" />
            <Setter Property="BorderThickness" Value="1.5" />
            <Setter Property="BorderBrush" Value="{DynamicResource NocturneAccent300Brush}" />
            <Setter Property="IsHitTestVisible" Value="False" />
            <Setter Property="Opacity" Value="0" />
        </Style>
        <Style Selector="Border.pulseRing.pulsing">
            <Style.Animations>
                <Animation Duration="0:0:1.8" IterationCount="INFINITE">
                    <KeyFrame Cue="0%">
                        <Setter Property="Opacity" Value="0.15" />
                    </KeyFrame>
                    <KeyFrame Cue="50%">
                        <Setter Property="Opacity" Value="0.9" />
                    </KeyFrame>
                    <KeyFrame Cue="100%">
                        <Setter Property="Opacity" Value="0.15" />
                    </KeyFrame>
                </Animation>
            </Style.Animations>
        </Style>
        <Style Selector="Border.nodeHeader">
            <Setter Property="Height" Value="19" />
            <Setter Property="Padding" Value="7,0" />
            <Setter Property="Background" Value="{DynamicResource NocturneNodeHeaderBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource NocturneNodeHeaderBorderBrush}" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
        </Style>
        <Style Selector="Border.nodeHeader.conditional">
            <Setter Property="Background" Value="{DynamicResource NocturneNodeConditionalHeaderBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource NocturneNodeConditionalHeaderBorderBrush}" />
        </Style>
        <Style Selector="Border.nodeHeader.running">
            <Setter Property="Background" Value="{DynamicResource NocturneNodeRunningHeaderBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource NocturneNodeRunningHeaderBorderBrush}" />
        </Style>
        <!-- The port dot. Its CENTRE sits exactly on the box's right edge, which is where
             CanvasEdgeRouter starts the line — the negative margin is load-bearing. -->
        <Style Selector="Border.port">
            <Setter Property="Width" Value="7" />
            <Setter Property="Height" Value="7" />
            <Setter Property="CornerRadius" Value="3.5" />
            <Setter Property="Margin" Value="6,0,-3.5,0" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Cursor" Value="Cross" />
            <Setter Property="Background" Value="{DynamicResource NocturneTextFaintBrush}" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
        <!-- Unwired: a hollow ring, so "this outcome ends the run" is visible from across
             the canvas and never reads as a missing connection. -->
        <Style Selector="Border.port.loose">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{DynamicResource NocturneEdgeBrush}" />
        </Style>
        <Style Selector="Border.port:pointerover">
            <Setter Property="Background" Value="{DynamicResource NocturneAccentBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource NocturneAccentBrush}" />
        </Style>
        <Style Selector="Border.keycap">
            <Setter Property="Height" Value="16" />
            <Setter Property="Padding" Value="7,0" />
            <Setter Property="CornerRadius" Value="3" />
            <Setter Property="Background" Value="{DynamicResource NocturneKeycapBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource NocturneNeutral800Brush}" />
            <Setter Property="BorderThickness" Value="1,1,1,2" />
            <Setter Property="HorizontalAlignment" Value="Left" />
        </Style>

        <!-- ============================ targets badge (1g) ============================
             Three tones, and the difference between them is the whole widget: neutral for
             "the context window", accent for "it hits something", danger for "it hits
             nothing". «0 окон» must never look like an ordinary number — but see
             TargetSelectorViewModel.BadgeIsDanger for why "the daemon has no windows at
             all" is deliberately NOT the danger case. -->
        <!-- No fixed height: the same pill has to sit inside a 19px node header and in the
             inspector, and a hard 22px (the mockup's standalone card) would burst the
             header. Padding gives it ~18px, which reads the same in both places. -->
        <Style Selector="Border.targetBadge">
            <Setter Property="MinHeight" Value="18" />
            <Setter Property="Padding" Value="8,1" />
            <Setter Property="CornerRadius" Value="{DynamicResource NocturneRadiusPill}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{DynamicResource NocturneDividerStrongBrush}" />
            <Setter Property="Background" Value="{DynamicResource NocturneFieldBackgroundBrush}" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style Selector="Border.targetBadge TextBlock.badgeText">
            <Setter Property="FontSize" Value="10.5" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
            <Setter Property="Foreground" Value="{DynamicResource NocturneTextMutedBrush}" />
        </Style>
        <Style Selector="Border.targetBadge.accent">
            <Setter Property="BorderBrush" Value="{DynamicResource NocturneChipInputBorderBrush}" />
            <Setter Property="Background" Value="{DynamicResource NocturneAccentTint15Brush}" />
        </Style>
        <Style Selector="Border.targetBadge.accent TextBlock.badgeText">
            <Setter Property="Foreground" Value="{DynamicResource NocturneChipTextSoftBrush}" />
        </Style>
        <Style Selector="Border.targetBadge.danger">
            <Setter Property="BorderBrush" Value="{DynamicResource NocturneDangerBorderHoverBrush}" />
            <Setter Property="Background" Value="{DynamicResource NocturneDangerSurfaceBrush}" />
        </Style>
        <Style Selector="Border.targetBadge.danger TextBlock.badgeText">
            <Setter Property="Foreground" Value="{DynamicResource NocturneDangerBrush}" />
        </Style>

        <!-- One window named in the expanded badge. `.missed` is a window the selector
             deliberately skips — struck through rather than hidden, because "who fell out"
             is half of what the expansion is for. -->
        <Style Selector="Border.windowChip">
            <Setter Property="Height" Value="19" />
            <Setter Property="Padding" Value="7,0" />
            <Setter Property="CornerRadius" Value="{DynamicResource NocturneRadiusSm}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{DynamicResource NocturneChipBorderSoftBrush}" />
            <Setter Property="Background" Value="{DynamicResource NocturneChipFillSoftBrush}" />
        </Style>
        <Style Selector="Border.windowChip.missed">
            <Setter Property="BorderBrush" Value="{DynamicResource NocturneDividerStrongBrush}" />
            <Setter Property="Background" Value="{DynamicResource NocturneFieldBackgroundBrush}" />
        </Style>
    </UserControl.Styles>

    <UserControl.Resources>

        <!-- ==== targets badge, collapsed (mockup 1g) ====
             «8 окон · кроме Склад». The number leads: what the tags are is already in the
             inspector, what this answers is how many windows the action will reach. The
             four bars are the share at a glance; a non-zero share always fills at least
             one, so "1 из 11" never renders as an empty strip. -->
        <DataTemplate x:Key="TargetBadgeTemplate" x:DataType="nodes:TargetSelectorViewModel">
            <Border Classes="targetBadge"
                    Classes.accent="{Binding BadgeIsAccent}"
                    Classes.danger="{Binding BadgeIsDanger}">
                <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                    <Ellipse Width="5" Height="5"
                             VerticalAlignment="Center"
                             IsVisible="{Binding ShowsHollowDot}"
                             Stroke="{DynamicResource NocturneNeutral600Brush}"
                             StrokeThickness="1" />
                    <Ellipse Width="5" Height="5"
                             VerticalAlignment="Center"
                             IsVisible="{Binding ShowsDangerDot}"
                             Fill="{DynamicResource NocturneDangerBrush}" />
                    <ItemsControl VerticalAlignment="Center"
                                  IsVisible="{Binding ShowsBars}"
                                  ItemsSource="{Binding Bars}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" Spacing="1.5" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="sys:Boolean">
                                <Border Width="3" Height="9" CornerRadius="1"
                                        Background="{Binding ., Converter={x:Static conv:TokenBrushConverter.Instance}, ConverterParameter=NocturneAccentBrush|NocturneChipInputBorderBrush}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock Classes="badgeText" Text="{Binding BadgeText}" />
                </StackPanel>
            </Border>
        </DataTemplate>

        <!-- ==== targets badge, compact ====
             The canvas variant. Same pill, same tones, count only: a 210px box already
             carries a family glyph and a type label, and the full «7 окон · кроме Лучник,
             Шаман» pushes the label out of the header entirely — which costs more than it
             buys, since the label is what says what the node DOES. The tags are in the
             tooltip, and in the inspector's badge one click away. -->
        <DataTemplate x:Key="TargetBadgeCompactTemplate" x:DataType="nodes:TargetSelectorViewModel">
            <Border Classes="targetBadge"
                    Classes.accent="{Binding BadgeIsAccent}"
                    Classes.danger="{Binding BadgeIsDanger}"
                    Padding="6,1"
                    ToolTip.Tip="{Binding BadgeText}">
                <StackPanel Orientation="Horizontal" Spacing="5" VerticalAlignment="Center">
                    <Ellipse Width="5" Height="5"
                             VerticalAlignment="Center"
                             IsVisible="{Binding ShowsHollowDot}"
                             Stroke="{DynamicResource NocturneNeutral600Brush}"
                             StrokeThickness="1" />
                    <Ellipse Width="5" Height="5"
                             VerticalAlignment="Center"
                             IsVisible="{Binding ShowsDangerDot}"
                             Fill="{DynamicResource NocturneDangerBrush}" />
                    <ItemsControl VerticalAlignment="Center"
                                  IsVisible="{Binding ShowsBars}"
                                  ItemsSource="{Binding Bars}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" Spacing="1.5" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="sys:Boolean">
                                <Border Width="3" Height="9" CornerRadius="1"
                                        Background="{Binding ., Converter={x:Static conv:TokenBrushConverter.Instance}, ConverterParameter=NocturneAccentBrush|NocturneChipInputBorderBrush}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock Classes="badgeText" FontSize="9.5" Text="{Binding BadgeCountText}" />
                </StackPanel>
            </Border>
        </DataTemplate>

        <!-- one require/exclude tag, removable. Declared BEFORE the popup that uses it:
             StaticResource inside a ResourceDictionary resolves against what is already
             above it. -->
        <DataTemplate x:Key="SelectorTagChipTemplate" x:DataType="nodes:SelectorTagChipViewModel">
            <Border Height="18"
                    Margin="0,0,4,2"
                    Padding="7,0,4,0"
                    CornerRadius="9"
                    BorderThickness="1"
                    BorderBrush="{DynamicResource NocturneWarningBorderBrush}"
                    Background="{DynamicResource NocturneWarningSurfaceBrush}">
                <StackPanel Orientation="Horizontal" Spacing="5">
                    <TextBlock VerticalAlignment="Center"
                               FontSize="{DynamicResource NocturneFontSizeMicro}"
                               Foreground="{DynamicResource NocturneWarningBrush}"
                               Text="{Binding Text}" />
                    <Button Classes="ghost"
                            Content="✕"
                            FontSize="8"
                            Padding="2,0"
                            VerticalAlignment="Center"
                            ToolTip.Tip="Убрать тег"
                            Click="OnRemoveSelectorTagClicked" />
                </StackPanel>
            </Border>
        </DataTemplate>

        <!-- one window in the expanded badge -->
        <DataTemplate x:Key="TargetWindowChipTemplate" x:DataType="nodes:TargetWindowChip">
            <Border Classes="windowChip"
                    Classes.missed="{Binding IsExcluded}"
                    Margin="0,0,4,4">
                <TextBlock VerticalAlignment="Center"
                           FontSize="9.5"
                           FontFamily="{DynamicResource NocturneFontFamilyMono}"
                           TextDecorations="{Binding IsExcluded, Converter={x:Static conv:StrikethroughConverter.Instance}}"
                           Foreground="{Binding IsExcluded, Converter={x:Static conv:TokenBrushConverter.Instance}, ConverterParameter=NocturneTextFaintBrush|NocturneChipTextSoftBrush}"
                           Text="{Binding Label}" />
            </Border>
        </DataTemplate>

        <!-- ==== targets badge, expanded ====
             A click opens the popup: the same selector as an editor of tag CHIPS (the
             comma-separated boxes are still the model, this is a friendlier face on them),
             and under it the live roster — who is hit, who is struck out, how many windows
             have no tags at all to route by. -->
        <DataTemplate x:Key="TargetBadgeButtonTemplate" x:DataType="nodes:TargetSelectorViewModel">
            <Button Classes="ghost"
                    Padding="0"
                    HorizontalAlignment="Left"
                    ToolTip.Tip="Кто попадёт под этот селектор прямо сейчас">
                <Button.Flyout>
                    <!-- Right-aligned, not centred: the badge sits at the right edge of a
                         308px inspector, and a Bottom-placed 322px popup is simply cut off
                         there — Avalonia hosts this one as an in-window overlay, so it does
                         not spill onto the desktop the way a platform popup would. -->
                    <Flyout Placement="BottomEdgeAlignedRight">
                        <StackPanel Width="322">

                            <!-- context vs tags: the two things a null-vs-empty selector
                                 means, which no pair of tag boxes can express. -->
                            <StackPanel Margin="10,8" Spacing="7">
                                <Grid ColumnDefinitions="*,*">
                                    <!-- Padding, not a fixed Height: the Button theme's
                                         11.2px vertical inset plus a 24px box leaves less
                                         room than an 11px line needs, and the descenders
                                         get shaved off. -->
                                    <Button Grid.Column="0"
                                            Content="контекст-окно"
                                            Padding="8,3"
                                            Margin="0,0,3,0"
                                            HorizontalAlignment="Stretch"
                                            HorizontalContentAlignment="Center"
                                            Classes.accent="{Binding !UseSelector}"
                                            Click="OnTargetContextClicked" />
                                    <Button Grid.Column="1"
                                            Content="по тегам"
                                            Padding="8,3"
                                            Margin="3,0,0,0"
                                            HorizontalAlignment="Stretch"
                                            HorizontalContentAlignment="Center"
                                            Classes.accent="{Binding UseSelector}"
                                            Click="OnTargetTagsClicked" />
                                </Grid>

                                <Grid ColumnDefinitions="60,*" IsVisible="{Binding UseSelector}">
                                    <TextBlock Grid.Column="0" Classes="label" VerticalAlignment="Top" Margin="0,5,0,0" Text="нужны" />
                                    <Border Grid.Column="1"
                                            MinHeight="26"
                                            Padding="6,3"
                                            CornerRadius="{DynamicResource NocturneRadiusSm}"
                                            BorderThickness="1"
                                            BorderBrush="{DynamicResource NocturneDividerStrongBrush}"
                                            Background="{DynamicResource NocturneFieldBackgroundBrush}">
                                        <StackPanel>
                                            <ItemsControl ItemsSource="{Binding RequireChips}"
                                                          ItemTemplate="{StaticResource SelectorTagChipTemplate}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <WrapPanel />
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                            </ItemsControl>
                                            <TextBox BorderThickness="0"
                                                     Background="Transparent"
                                                     Padding="0"
                                                     MinHeight="18"
                                                     FontSize="{DynamicResource NocturneFontSizeMicro}"
                                                     Watermark="+ тег"
                                                     Text="{Binding NewRequireTag, Mode=TwoWay}"
                                                     KeyDown="OnRequireTagKeyDown" />
                                        </StackPanel>
                                    </Border>
                                </Grid>

                                <Grid ColumnDefinitions="60,*" IsVisible="{Binding UseSelector}">
                                    <TextBlock Grid.Column="0" Classes="label" VerticalAlignment="Top" Margin="0,5,0,0" Text="исключить" />
                                    <Border Grid.Column="1"
                                            MinHeight="26"
                                            Padding="6,3"
                                            CornerRadius="{DynamicResource NocturneRadiusSm}"
                                            BorderThickness="1"
                                            BorderBrush="{DynamicResource NocturneChipInputBorderBrush}"
                                            Background="{DynamicResource NocturneFieldBackgroundBrush}">
                                        <StackPanel>
                                            <ItemsControl ItemsSource="{Binding ExcludeChips}"
                                                          ItemTemplate="{StaticResource SelectorTagChipTemplate}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <WrapPanel />
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                            </ItemsControl>
                                            <TextBox BorderThickness="0"
                                                     Background="Transparent"
                                                     Padding="0"
                                                     MinHeight="18"
                                                     FontSize="{DynamicResource NocturneFontSizeMicro}"
                                                     Watermark="+ тег"
                                                     Text="{Binding NewExcludeTag, Mode=TwoWay}"
                                                     KeyDown="OnExcludeTagKeyDown" />
                                        </StackPanel>
                                    </Border>
                                </Grid>

                                <TextBlock Classes="hint"
                                           TextWrapping="Wrap"
                                           IsVisible="{Binding !UseSelector}"
                                           Text="Селектор пуст — действие уйдёт в окно, из которого запущен макрос." />
                            </StackPanel>

                            <!-- The roster. Tag-routing only: in context mode the selector
                                 targets exactly one window — whichever the run came from —
                                 and a list of eight struck-out names under «0 из 8» would
                                 say the opposite of what actually happens. -->
                            <Border BorderThickness="0,1,0,0"
                                    BorderBrush="{DynamicResource NocturneDividerStrongBrush}"
                                    Background="{DynamicResource NocturneBgBrush}"
                                    IsVisible="{Binding UseSelector}"
                                    Padding="10,7">
                                <StackPanel Spacing="5">
                                    <StackPanel Orientation="Horizontal" Spacing="7">
                                        <TextBlock Classes="eyebrow" Text="СЕЙЧАС ПОПАДАЕТ" />
                                        <TextBlock FontSize="{DynamicResource NocturneFontSizeMicro}"
                                                   VerticalAlignment="Center"
                                                   FontFamily="{DynamicResource NocturneFontFamilyMono}"
                                                   Foreground="{DynamicResource NocturneAccent400Brush}"
                                                   Text="{Binding HitText}" />
                                    </StackPanel>
                                    <ItemsControl ItemsSource="{Binding HitWindows}"
                                                  ItemTemplate="{StaticResource TargetWindowChipTemplate}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                    </ItemsControl>
                                    <StackPanel Orientation="Horizontal" Spacing="4">
                                        <Border Classes="windowChip" IsVisible="{Binding HasMore}">
                                            <TextBlock VerticalAlignment="Center"
                                                       FontSize="9.5"
                                                       FontFamily="{DynamicResource NocturneFontFamilyMono}"
                                                       Foreground="{DynamicResource NocturneChipTextSoftBrush}"
                                                       Text="{Binding MoreText}" />
                                        </Border>
                                        <Border Classes="windowChip missed" IsVisible="{Binding HasUntagged}">
                                            <TextBlock VerticalAlignment="Center"
                                                       FontSize="9.5"
                                                       FontFamily="{DynamicResource NocturneFontFamilyMono}"
                                                       Foreground="{DynamicResource NocturneTextFaintBrush}"
                                                       Text="{Binding UntaggedText}" />
                                        </Border>
                                    </StackPanel>
                                    <ItemsControl ItemsSource="{Binding MissedWindows}"
                                                  ItemTemplate="{StaticResource TargetWindowChipTemplate}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Flyout>
                </Button.Flyout>
                <ContentControl Content="{Binding}" ContentTemplate="{StaticResource TargetBadgeTemplate}" />
            </Button>
        </DataTemplate>

        <!-- ==== one outcome row on a COLLAPSED box: caption + port dot ==== -->
        <DataTemplate x:Key="OutcomePortTemplate" x:DataType="nodes:NodeEdgeViewModel">
            <Grid ColumnDefinitions="*,Auto" Height="15">
                <TextBlock Grid.Column="0"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center"
                           FontSize="9.5"
                           TextTrimming="CharacterEllipsis"
                           Foreground="{Binding IsEnd, Converter={x:Static conv:TokenBrushConverter.Instance}, ConverterParameter=NocturneTextFaintBrush|NocturneAccent400Brush}"
                           Text="{Binding BoxLabel}" />
                <Border Grid.Column="1"
                        Classes="port"
                        Classes.loose="{Binding IsEnd}"
                        ToolTip.Tip="Потяните, чтобы связать с нодой; бросьте на пустое место — конец прогона"
                        PointerPressed="OnPortPressed" />
            </Grid>
        </DataTemplate>

        <!-- ==== one outcome as an EDITOR: caption + drop-down of node ids ==== -->
        <DataTemplate x:Key="EdgeEditorTemplate" x:DataType="nodes:NodeEdgeViewModel">
            <!-- 82px, not the 64 of the parameter rows: "не распознано" is the longest
                 outcome name in the catalogue and a trimmed one reads as a different word. -->
            <Grid ColumnDefinitions="82,*" Margin="0,0,0,5.6">
                <TextBlock Grid.Column="0" Classes="label" Text="{Binding ShortLabel}" />
                <ComboBox Grid.Column="1"
                          HorizontalAlignment="Stretch"
                          ItemsSource="{Binding Choices}"
                          SelectedItem="{Binding TargetId, Mode=TwoWay}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate x:DataType="sys:String">
                            <TextBlock Text="{Binding ., Converter={x:Static conv:NodeIdDisplayConverter.Instance}}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </Grid>
        </DataTemplate>

        <!-- ==== the node box ==== -->
        <DataTemplate x:Key="NodeBoxTemplate" x:DataType="nodes:NodeRowViewModel">
            <Border Classes="node"
                    Classes.selected="{Binding IsSelected}"
                    Classes.passed="{Binding IsPassed}"
                    Classes.running="{Binding IsExecuting}"
                    Classes.paused="{Binding IsPaused}"
                    Classes.varSource="{Binding IsVariableSource}"
                    Classes.varConsumer="{Binding IsVariableConsumer}"
                    Width="{Binding BoxWidth}"
                    Height="{Binding BoxHeight}"
                    PointerPressed="OnNodePressed"
                    DoubleTapped="OnNodeDoubleTapped">
                <Grid RowDefinitions="Auto,*,Auto">

                    <!-- header: family glyph, type, targets chip -->
                    <Border Grid.Row="0"
                            Classes="nodeHeader"
                            Classes.conditional="{Binding IsConditional}"
                            Classes.running="{Binding IsExecuting}">
                        <Grid ColumnDefinitions="Auto,*,Auto">
                            <Border Grid.Column="0"
                                    Width="7" Height="7"
                                    CornerRadius="2"
                                    VerticalAlignment="Center"
                                    Background="{DynamicResource NocturneTextFaintBrush}"
                                    IsVisible="{Binding !IsConditional}" />
                            <Border Grid.Column="0"
                                    Width="7" Height="7"
                                    VerticalAlignment="Center"
                                    RenderTransform="rotate(45deg)"
                                    BorderThickness="1"
                                    BorderBrush="{DynamicResource NocturneAccentBrush}"
                                    Background="{DynamicResource NocturneAccentTint22Brush}"
                                    IsVisible="{Binding IsConditional}" />
                            <TextBlock Grid.Column="1"
                                       Margin="6,0,0,0"
                                       FontSize="10.5"
                                       FontWeight="Medium"
                                       VerticalAlignment="Center"
                                       TextTrimming="CharacterEllipsis"
                                       Foreground="{Binding IsConditional, Converter={x:Static conv:TokenBrushConverter.Instance}, ConverterParameter=NocturneChipTextSoftBrush|NocturneNeutral300Brush}"
                                       Text="{Binding TypeLabel}" />
                            <!-- The live targets badge (1g), compact. Only for nodes that
                                 route by TAGS: "acts on the context window" is the default
                                 every second node has — the departure from it is what earns
                                 a word. Display-only here; the clickable, expanding copy is
                                 in the inspector, which is not inside the canvas's
                                 scale+translate transform (a Popup over one lands where the
                                 transform is not). -->
                            <ContentControl Grid.Column="2"
                                            Margin="6,0,0,0"
                                            IsVisible="{Binding ShowsTargetChip}"
                                            Content="{Binding Target}"
                                            ContentTemplate="{StaticResource TargetBadgeCompactTemplate}" />
                            <!-- The run stamp replaces the targets chip while a walk is being
                                 watched: ✓ with the time for a passed node, ▸ for the live
                                 one. Same slot on purpose — the header is 210px wide and the
                                 type label is what must never be squeezed out. -->
                            <TextBlock Grid.Column="2"
                                       Margin="6,0,0,0"
                                       FontSize="10"
                                       VerticalAlignment="Center"
                                       IsVisible="{Binding IsPassed}"
                                       ToolTip.Tip="{Binding PassedOutcome}"
                                       Foreground="{DynamicResource NocturneAccentBrush}"
                                       Text="{Binding PassedTime, StringFormat='✓ {0}'}" />
                            <TextBlock Grid.Column="2"
                                       Margin="6,0,0,0"
                                       FontSize="10"
                                       FontFamily="{DynamicResource NocturneFontFamilyMono}"
                                       VerticalAlignment="Center"
                                       IsVisible="{Binding IsPaused}"
                                       Foreground="{DynamicResource NocturneDangerBrush}"
                                       Text="&#x25A0;" />
                        </Grid>
                    </Border>

                    <!-- collapsed body: id + the one line that says what this node does -->
                    <StackPanel Grid.Row="1" Margin="7,3,7,0" IsVisible="{Binding !IsExpanded}">
                        <TextBlock FontSize="9.5"
                                   FontFamily="{DynamicResource NocturneFontFamilyMono}"
                                   Foreground="{DynamicResource NocturneTextMutedBrush}"
                                   TextTrimming="CharacterEllipsis"
                                   Text="{Binding NodeId}" />
                        <TextBlock Margin="0,1,0,0"
                                   FontSize="10.5"
                                   FontFamily="{DynamicResource NocturneFontFamilyMono}"
                                   Foreground="{DynamicResource NocturneNeutral300Brush}"
                                   TextTrimming="CharacterEllipsis"
                                   IsVisible="{Binding HasSummary}"
                                   Text="{Binding Summary}" />
                        <Border Classes="keycap"
                                Margin="0,1,0,0"
                                IsVisible="{Binding Keycap, Converter={x:Static conv:NotNullToBoolConverter.Instance}}">
                            <TextBlock FontSize="10"
                                       FontWeight="Medium"
                                       FontFamily="{DynamicResource NocturneFontFamilyMono}"
                                       VerticalAlignment="Center"
                                       Text="{Binding Keycap}" />
                        </Border>
                    </StackPanel>

                    <!-- expanded body (1e): the box becomes an editor of itself -->
                    <StackPanel Grid.Row="1" Margin="9,8,9,4" Spacing="5.6" IsVisible="{Binding IsExpanded}">
                        <Grid ColumnDefinitions="64,*">
                            <TextBlock Grid.Column="0" Classes="label" Text="id" />
                            <TextBox Grid.Column="1"
                                     Classes="mono"
                                     Text="{Binding NodeId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                        </Grid>
                        <ContentControl Content="{Binding}" />
                        <ContentControl Content="{Binding Target}" IsVisible="{Binding HasTarget}" />
                    </StackPanel>

                    <!-- outcomes: port rows when collapsed, drop-downs when expanded -->
                    <ItemsControl Grid.Row="2"
                                  Margin="0,0,0,4"
                                  IsVisible="{Binding !IsExpanded}"
                                  ItemsSource="{Binding Edges}"
                                  ItemTemplate="{StaticResource OutcomePortTemplate}" />
                    <Border Grid.Row="2"
                            Padding="9,6,9,4"
                            IsVisible="{Binding IsExpanded}"
                            Background="{DynamicResource NocturneSurfaceRowAltBrush}"
                            BorderThickness="0,1,0,0"
                            BorderBrush="{DynamicResource NocturneDividerStrongBrush}">
                        <ItemsControl ItemsSource="{Binding Edges}"
                                      ItemTemplate="{StaticResource EdgeEditorTemplate}" />
                    </Border>

                    <!-- Spans every row so the bar runs the full height of the box. Last in
                         the Grid, so it paints over the header's background. -->
                    <Border Grid.Row="0" Grid.RowSpan="3"
                            Classes="breakpointBar"
                            IsVisible="{Binding HasBreakpoint}"
                            ToolTip.Tip="Брейкпоинт: прогон встанет ПЕРЕД этой нодой" />

                    <!-- Only while the walk is actually running this node: a PARKED walk gets
                         the static danger edge instead, because "stopped here" and "running
                         here" must not share an animation. -->
                    <Border Grid.Row="0" Grid.RowSpan="3"
                            Classes="pulseRing"
                            Classes.pulsing="{Binding IsRunningLive}" />
                </Grid>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <UserControl.DataTemplates>

        <!-- ==== shared editors, used by BOTH the inspector and an expanded box ==== -->

        <DataTemplate DataType="{x:Type nodes:TargetSelectorViewModel}" x:DataType="nodes:TargetSelectorViewModel">
            <StackPanel Spacing="2.8">
                <CheckBox Content="Цели по тегам (выкл. — контекст-окно)"
                          IsChecked="{Binding UseSelector, Mode=TwoWay}" />
                <Grid ColumnDefinitions="64,*" IsVisible="{Binding UseSelector}" Margin="0,2.8,0,0">
                    <TextBlock Grid.Column="0" Classes="label" Text="нужны" />
                    <TextBox Grid.Column="1" Watermark="через запятую" Text="{Binding RequireText, Mode=TwoWay}" />
                </Grid>
                <Grid ColumnDefinitions="64,*" IsVisible="{Binding UseSelector}">
                    <TextBlock Grid.Column="0" Classes="label" Text="исключить" />
                    <TextBox Grid.Column="1" Watermark="через запятую" Text="{Binding ExcludeText, Mode=TwoWay}" />
                </Grid>
            </StackPanel>
        </DataTemplate>

        <DataTemplate DataType="{x:Type nodes:RegionEditorViewModel}" x:DataType="nodes:RegionEditorViewModel">
            <StackPanel>
                <Grid ColumnDefinitions="64,*">
                    <TextBlock Grid.Column="0" Classes="label" Text="регион" />
                    <Grid Grid.Column="1" ColumnDefinitions="*,*,*,*">
                        <TextBox Grid.Column="0" Classes="numeric" Margin="0,0,2,0" Text="{Binding XText, Mode=TwoWay}" />
                        <TextBox Grid.Column="1" Classes="numeric" Margin="2,0" Text="{Binding YText, Mode=TwoWay}" />
                        <TextBox Grid.Column="2" Classes="numeric" Margin="2,0" Text="{Binding WidthText, Mode=TwoWay}" />
                        <TextBox Grid.Column="3" Classes="numeric" Margin="2,0,0,0" Text="{Binding HeightText, Mode=TwoWay}" />
                    </Grid>
                </Grid>
                <Grid ColumnDefinitions="64,*" Margin="0,1,0,0">
                    <Grid Grid.Column="1" ColumnDefinitions="*,*,*,*">
                        <TextBlock Grid.Column="0" Classes="hint" HorizontalAlignment="Center" Text="X" />
                        <TextBlock Grid.Column="1" Classes="hint" HorizontalAlignment="Center" Text="Y" />
                        <TextBlock Grid.Column="2" Classes="hint" HorizontalAlignment="Center" Text="Ш" />
                        <TextBlock Grid.Column="3" Classes="hint" HorizontalAlignment="Center" Text="В" />
                    </Grid>
                </Grid>
            </StackPanel>
        </DataTemplate>

        <!-- ==== node parameter blocks (one per node type) ==== -->

        <DataTemplate DataType="{x:Type nodes:KeyPressNodeRowViewModel}" x:DataType="nodes:KeyPressNodeRowViewModel">
            <Grid ColumnDefinitions="64,*">
                <TextBlock Grid.Column="0" Classes="label" Text="клавиша" />
                <controls:KeyBindingPicker Grid.Column="1"
                                           HorizontalAlignment="Stretch"
                                           CaptureModifiers="False"
                                           Key="{Binding KeyName, Mode=TwoWay}" />
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type nodes:ClickNodeRowViewModel}" x:DataType="nodes:ClickNodeRowViewModel">
            <StackPanel Spacing="5.6">
                <Grid ColumnDefinitions="64,*" IsVisible="{Binding UseLiteralPoint}">
                    <TextBlock Grid.Column="0" Classes="label" Text="точка" />
                    <Grid Grid.Column="1" ColumnDefinitions="*,*">
                        <TextBox Grid.Column="0" Classes="numeric" Margin="0,0,2,0" Text="{Binding XText, Mode=TwoWay}" />
                        <TextBox Grid.Column="1" Classes="numeric" Margin="2,0,0,0" Text="{Binding YText, Mode=TwoWay}" />
                    </Grid>
                </Grid>
                <Grid ColumnDefinitions="64,*" IsVisible="{Binding UseVariable}">
                    <TextBlock Grid.Column="0" Classes="label" Text="из перем." />
                    <TextBox Grid.Column="1" Classes="mono" Watermark="cursor" Text="{Binding PointVar, Mode=TwoWay}" />
                </Grid>
                <StackPanel Orientation="Horizontal" Spacing="11.2">
                    <CheckBox Content="из переменной" IsChecked="{Binding UseVariable, Mode=TwoWay}" />
                    <CheckBox Content="двойной" IsChecked="{Binding DoubleClick, Mode=TwoWay}" />
                </StackPanel>
            </StackPanel>
        </DataTemplate>

        <DataTemplate DataType="{x:Type nodes:DelayNodeRowViewModel}" x:DataType="nodes:DelayNodeRowViewModel">
            <StackPanel Spacing="2.8">
                <Grid ColumnDefinitions="64,*">
                    <TextBlock Grid.Column="0" Classes="label" Text="пауза" />
                    <Grid Grid.Column="1" ColumnDefinitions="*,Auto">
                        <TextBox Grid.Column="0" Classes="mono" Text="{Binding SecondsText, Mode=TwoWay}" />
                        <TextBlock Grid.Column="1" Classes="hint" Margin="5.6,0,0,0" Text="сек" />
                    </Grid>
                </Grid>
                <TextBlock Classes="hint" Text="дробное — можно: 1.5 = 1500 мс" Margin="64,0,0,0" />
            </StackPanel>
        </DataTemplate>

        <DataTemplate DataType="{x:Type nodes:TagNodeRowViewModel}" x:DataType="nodes:TagNodeRowViewModel">
            <Grid ColumnDefinitions="64,*">
                <TextBlock Grid.Column="0" Classes="label" Text="тег" />
                <TextBox Grid.Column="1" Watermark="имя тега или {переменная}" Text="{Binding Tag, Mode=TwoWay}" />
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type nodes:SetIconNodeRowViewModel}" x:DataType="nodes:SetIconNodeRowViewModel">
            <Grid ColumnDefinitions="64,*">
                <TextBlock Grid.Column="0" Classes="label" Text="иконка" />
                <TextBox Grid.Column="1"
                         Classes="mono"
                         Watermark="Assets/ClassIcons/{tag}.png"
                         Text="{Binding IconPath, Mode=TwoWay}" />
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type nodes:RunMacroNodeRowViewModel}" x:DataType="nodes:RunMacroNodeRowViewModel">
            <StackPanel Spacing="5.6">
                <Grid ColumnDefinitions="64,*">
                    <TextBlock Grid.Column="0" Classes="label" Text="макрос" />
                    <ComboBox Grid.Column="1"
                              HorizontalAlignment="Stretch"
                              ItemsSource="{Binding MacroChoices}"
                              SelectedItem="{Binding MacroName, Mode=TwoWay}" />
                </Grid>
                <CheckBox Content="ждать завершения" IsChecked="{Binding Await, Mode=TwoWay}" Margin="64,0,0,0" />
            </StackPanel>
        </DataTemplate>

        <DataTemplate DataType="{x:Type nodes:FindElementNodeRowViewModel}" x:DataType="nodes:FindElementNodeRowViewModel">
            <StackPanel Spacing="5.6">
                <Grid ColumnDefinitions="64,*">
                    <TextBlock Grid.Column="0" Classes="label" Text="шаблон" />
                    <TextBox Grid.Column="1" Classes="mono" Watermark="имя файла без .png" Text="{Binding Template, Mode=TwoWay}" />
                </Grid>
                <ContentControl Content="{Binding Region}" />
                <TextBlock Classes="hint" Text="Ш или В = 0 — искать по всему окну" Margin="64,0,0,0" />
                <Grid ColumnDefinitions="64,*">
                    <TextBlock Grid.Column="0" Classes="label" Text="точка →" />
                    <TextBox Grid.Column="1" Classes="mono" Watermark="переменная (необяз.)" Text="{Binding FoundPointVar, Mode=TwoWay}" />
                </Grid>
            </StackPanel>
        </DataTemplate>

        <DataTemplate DataType="{x:Type nodes:WaitForElementNodeRowViewModel}" x:DataType="nodes:WaitForElementNodeRowViewModel">
            <StackPanel Spacing="5.6">
                <Grid ColumnDefinitions="64,*">
                    <TextBlock Grid.Column="0" Classes="label" Text="шаблон" />
                    <TextBox Grid.Column="1" Classes="mono" Watermark="имя файла без .png" Text="{Binding Template, Mode=TwoWay}" />
                </Grid>
                <ContentControl Content="{Binding Region}" />
                <Grid ColumnDefinitions="64,*">
                    <TextBlock Grid.Column="0" Classes="label" Text="таймаут" />
                    <Grid Grid.Column="1" ColumnDefinitions="*,Auto">
                        <TextBox Grid.Column="0" Classes="mono" Text="{Binding TimeoutMsText, Mode=TwoWay}" />
                        <TextBlock Grid.Column="1" Classes="hint" Margin="5.6,0,0,0" Text="мс" />
                    </Grid>
                </Grid>
                <Grid ColumnDefinitions="64,*">
                    <TextBlock Grid.Column="0" Classes="label" Text="точка →" />
                    <TextBox Grid.Column="1" Classes="mono" Watermark="переменная (необяз.)" Text="{Binding FoundPointVar, Mode=TwoWay}" />
                </Grid>
            </StackPanel>
        </DataTemplate>

        <DataTemplate DataType="{x:Type nodes:RecognizeTagNodeRowViewModel}" x:DataType="nodes:RecognizeTagNodeRowViewModel">
            <StackPanel Spacing="5.6">
                <Grid ColumnDefinitions="64,*">
                    <TextBlock Grid.Column="0" Classes="label" Text="набор" />
                    <TextBox Grid.Column="1" Classes="mono" Watermark="classes" Text="{Binding TemplateSet, Mode=TwoWay}" />
                </Grid>
                <ContentControl Content="{Binding Region}" />
                <Grid ColumnDefinitions="64,*">
                    <TextBlock Grid.Column="0" Classes="label" Text="результат" />
                    <TextBox Grid.Column="1" Classes="mono" Watermark="tag" Text="{Binding ResultVar, Mode=TwoWay}" />
                </Grid>
                <CheckBox Content="вешать тег на окно" IsChecked="{Binding ApplyTag, Mode=TwoWay}" Margin="64,0,0,0" />
            </StackPanel>
        </DataTemplate>

        <!-- ==== trigger rows ==== -->

        <!-- The hotkey trap, mockup 1f. The picker itself carries the four states; what
             belongs OUTSIDE it is the two lines of guidance under the armed state — how to
             get out of capture, and the fact that the daemon's global hotkeys are down
             while this mode is open. Both are bound to the picker's own IsCapturing, so
             they appear exactly while they are true. -->
        <DataTemplate DataType="{x:Type nodes:HotkeyTriggerRowViewModel}" x:DataType="nodes:HotkeyTriggerRowViewModel">
            <StackPanel Margin="0,0,0,5.6">
                <Grid ColumnDefinitions="*,Auto">
                    <controls:KeyBindingPicker Grid.Column="0"
                                               x:Name="Picker"
                                               HorizontalAlignment="Stretch"
                                               CaptureModifiers="True"
                                               Conflict="{Binding Conflict}"
                                               Key="{Binding KeyName, Mode=TwoWay}"
                                               Modifiers="{Binding Modifiers, Mode=TwoWay}"
                                               MouseButton="{Binding MouseButton, Mode=TwoWay}" />
                    <Button Grid.Column="1"
                            Content="✕"
                            Classes="ghost"
                            FontSize="10"
                            Width="24"
                            Height="24"
                            Margin="5.6,0,0,0"
                            HorizontalContentAlignment="Center"
                            ToolTip.Tip="Убрать триггер (Del в ловушке — очистить сочетание)"
                            Click="OnRemoveTriggerClicked" />
                </Grid>
                <StackPanel Margin="0,4,0,0" Spacing="2" IsVisible="{Binding #Picker.IsCapturing}">
                    <TextBlock Classes="hint"
                               TextWrapping="Wrap"
                               Text="Esc — отмена · Del — очистить · ловятся и боковые кнопки мыши" />
                    <!-- A Grid, not a horizontal StackPanel: a StackPanel measures its
                         children with infinite width, so TextWrapping never fires and the
                         line is simply cut off at the panel edge. -->
                    <Grid ColumnDefinitions="Auto,*">
                        <!-- U+25B2, NOT U+26A0 ⚠: the warning sign has an emoji
                             presentation, so Windows serves it from Segoe UI Emoji as a
                             full-colour pictogram that ignores Foreground entirely — the
                             same trap D1 hit with U+25B6. -->
                        <TextBlock Grid.Column="0"
                                   FontSize="9"
                                   Margin="0,0,6,0"
                                   VerticalAlignment="Center"
                                   Foreground="{DynamicResource NocturneWarningBrush}"
                                   Text="&#x25B2;" />
                        <TextBlock Grid.Column="1"
                                   FontSize="{DynamicResource NocturneFontSizeMicro}"
                                   VerticalAlignment="Center"
                                   TextWrapping="Wrap"
                                   Foreground="{DynamicResource NocturneWarningBrush}"
                                   Text="глобальные хоткеи приостановлены, пока открыт редактор" />
                    </Grid>
                </StackPanel>
            </StackPanel>
        </DataTemplate>

        <DataTemplate DataType="{x:Type nodes:ProcessTriggerRowViewModel}" x:DataType="nodes:ProcessTriggerRowViewModel">
            <Grid ColumnDefinitions="*,Auto" Margin="0,0,0,2.8">
                <TextBox Grid.Column="0"
                         Classes="mono"
                         Watermark="elementclient_64"
                         Text="{Binding ProcessName, Mode=TwoWay}" />
                <Button Grid.Column="1"
                        Content="×"
                        Classes="ghost"
                        Margin="5.6,0,0,0"
                        ToolTip.Tip="Убрать триггер"
                        Click="OnRemoveTriggerClicked" />
            </Grid>
        </DataTemplate>

        <!-- ==== validation issue ==== -->

        <DataTemplate DataType="{x:Type vm:ValidationIssueViewModel}" x:DataType="vm:ValidationIssueViewModel">
            <Button Classes="ghost"
                    Padding="2.8,1.4"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Left"
                    Click="OnIssueClicked">
                <TextBlock FontSize="{DynamicResource NocturneFontSizeSmall}"
                           TextWrapping="Wrap"
                           Foreground="{Binding IsError, Converter={x:Static conv:IssueSeverityToBrushConverter.Instance}}"
                           Text="{Binding Display}" />
            </Button>
        </DataTemplate>
    </UserControl.DataTemplates>

    <Grid RowDefinitions="Auto,*">

        <!-- =============================== toolbar =============================== -->
        <Border Grid.Row="0"
                Background="{DynamicResource NocturneSurfaceRaisedBrush}"
                BorderBrush="{DynamicResource NocturneDividerBrush}"
                BorderThickness="0,0,0,1"
                Padding="12,8">
            <Grid ColumnDefinitions="Auto,Auto,Auto,Auto,Auto,Auto,*,Auto,Auto,Auto,Auto">
                <TextBox Grid.Column="0"
                         Width="170"
                         Height="26"
                         FontWeight="Medium"
                         FontSize="{DynamicResource NocturneFontSizeBody}"
                         IsEnabled="{Binding HasOpenMacro}"
                         ToolTip.Tip="Имя макроса = имя файла"
                         Text="{Binding MacroName, Mode=TwoWay}" />

                <Border Grid.Column="1" Width="1" Height="18" Margin="10,0"
                        Background="{DynamicResource NocturneDividerStrongBrush}" />

                <TextBlock Grid.Column="2" Classes="label" Text="старт" Margin="0,0,5.6,0" />
                <ComboBox Grid.Column="3"
                          MinWidth="150"
                          IsEnabled="{Binding HasOpenMacro}"
                          ItemsSource="{Binding StartNodeChoices}"
                          SelectedItem="{Binding StartNodeId, Mode=TwoWay}" />

                <Button Grid.Column="4"
                        x:Name="AddNodeButton"
                        Content="+ Нода"
                        Height="26"
                        Margin="10,0,0,0"
                        IsEnabled="{Binding HasOpenMacro}">
                    <Button.Flyout>
                        <Flyout Placement="Bottom">
                            <ItemsControl ItemsSource="{Binding NodeKinds}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="nodes:MacroNodeKindOption">
                                        <Button Content="{Binding Label}"
                                                Classes="ghost"
                                                Padding="{DynamicResource NocturneInsetField}"
                                                MinWidth="180"
                                                HorizontalAlignment="Stretch"
                                                HorizontalContentAlignment="Left"
                                                Click="OnAddNodeClicked" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Flyout>
                    </Button.Flyout>
                </Button>

                <!-- ==== debugger (D5) ====
                     Present only once a walk of this graph is on record: four permanently
                     dead buttons is how a toolbar stops being read. The mockup puts the
                     progress counter inside the run chip; here it sits with the controls it
                     is about, and the WALK PICKER stays in the log strip below with the
                     ◂ ▸ arrows — two different numbers, each next to what it counts. -->
                <StackPanel Grid.Column="5"
                            Orientation="Horizontal"
                            Spacing="4"
                            VerticalAlignment="Center"
                            Margin="10,0,0,0"
                            IsVisible="{Binding HasDebugTarget}">
                    <Border Width="1" Height="18" Margin="0,0,6,0"
                            Background="{DynamicResource NocturneDividerStrongBrush}" />

                    <!-- Pause and Resume share a slot: they are mutually exclusive states of
                         one walk, and a 1520px toolbar has no room for a permanently dead
                         twin. The mockup's ⏸ / ⤼ / ▷| glyphs are NOT used — U+23F8 carries
                         Emoji_Presentation and would arrive as a colour pictogram ignoring
                         Foreground, the same trap D1 hit on U+25B6 and D4 on U+26A0. U+25B8
                         and U+25A0 are the two that were verified to render as text. -->
                    <Button Height="26"
                            Content="Пауза"
                            IsVisible="{Binding !SelectedRunIsPaused}"
                            IsEnabled="{Binding CanPause}"
                            ToolTip.Tip="Встать на паузу перед следующей нодой этого обхода"
                            Click="OnDebugPauseClicked" />
                    <Button Height="26"
                            Classes="accent"
                            Content="&#x25B8; Дальше"
                            IsVisible="{Binding SelectedRunIsPaused}"
                            IsEnabled="{Binding CanResume}"
                            ToolTip.Tip="Снять обход с паузы"
                            Click="OnDebugResumeClicked" />
                    <Button Height="26"
                            Content="Шаг"
                            IsEnabled="{Binding CanResume}"
                            ToolTip.Tip="Выполнить одну ноду и встать снова"
                            Click="OnDebugStepClicked" />
                    <Button Height="26"
                            Content="До курсора"
                            IsEnabled="{Binding CanRunToCursor}"
                            ToolTip.Tip="Идти до ноды, выбранной на canvas, и встать перед ней"
                            Click="OnDebugRunToCursorClicked" />
                    <Button Height="26"
                            Classes="danger"
                            Content="{Binding StopLabel}"
                            IsEnabled="{Binding CanStop}"
                            ToolTip.Tip="{Binding StopTooltip}"
                            Click="OnDebugStopClicked" />

                    <!-- Counter and clock only. The walk's IDENTITY and its pause reason are
                         in the log strip's header, next to the ◂ ▸ that choose it — printing
                         them twice cost 260px and pushed «Сохранить» off the window edge,
                         which is exactly the button you reach for after a debugging session. -->
                    <Border Height="26"
                            Margin="6,0,0,0"
                            Padding="8,0"
                            CornerRadius="{DynamicResource NocturneRadiusSm}"
                            Background="{DynamicResource NocturneSurfaceInsetBrush}"
                            BorderBrush="{DynamicResource NocturneDividerStrongBrush}"
                            BorderThickness="1"
                            ToolTip.Tip="Пройдено нод / всего в графе · время обхода">
                        <StackPanel Orientation="Horizontal" Spacing="7" VerticalAlignment="Center">
                            <TextBlock FontSize="10.5"
                                       VerticalAlignment="Center"
                                       FontFamily="{DynamicResource NocturneFontFamilyMono}"
                                       Text="{Binding RunProgressText}" />
                            <TextBlock FontSize="10.5"
                                       VerticalAlignment="Center"
                                       FontFamily="{DynamicResource NocturneFontFamilyMono}"
                                       Foreground="{DynamicResource NocturneTextFaintBrush}"
                                       Text="{Binding RunElapsedText}" />
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Feedback lives in the middle of the bar: it is about the whole macro,
                     not about whatever the inspector happens to be showing. -->
                <StackPanel Grid.Column="6" Margin="14,0" VerticalAlignment="Center" Spacing="1">
                    <TextBlock FontSize="{DynamicResource NocturneFontSizeMicro}"
                               Foreground="{DynamicResource NocturneWarningBrush}"
                               TextTrimming="CharacterEllipsis"
                               IsVisible="{Binding ChangedOnDisk}"
                               ToolTip.Tip="«Перечитать» — взять версию с диска, «Сохранить» — затереть её"
                               Text="Макрос изменён на диске, а здесь есть несохранённые правки." />
                    <TextBlock FontSize="{DynamicResource NocturneFontSizeMicro}"
                               Foreground="{DynamicResource NocturneDangerBrush}"
                               TextTrimming="CharacterEllipsis"
                               IsVisible="{Binding ErrorMessage, Converter={x:Static conv:NotNullToBoolConverter.Instance}}"
                               Text="{Binding ErrorMessage}" />
                    <TextBlock FontSize="{DynamicResource NocturneFontSizeMicro}"
                               Foreground="{DynamicResource NocturneTextFaintBrush}"
                               TextTrimming="CharacterEllipsis"
                               IsVisible="{Binding StatusMessage, Converter={x:Static conv:NotNullToBoolConverter.Instance}}"
                               Text="{Binding StatusMessage}" />
                </StackPanel>

                <Button Grid.Column="7"
                        Content="Папка макросов"
                        Height="26"
                        ToolTip.Tip="{Binding FolderPath}"
                        Click="OnOpenFolderClicked" />
                <Button Grid.Column="8"
                        Content="Перечитать"
                        Height="26"
                        Margin="6,0,0,0"
                        IsEnabled="{Binding HasOpenMacro}"
                        ToolTip.Tip="Отбросить правки и взять версию с диска"
                        Click="OnReloadClicked" />
                <Button Grid.Column="9"
                        Content="Авто-раскладка"
                        Height="26"
                        Margin="6,0,0,0"
                        IsEnabled="{Binding HasOpenMacro}"
                        ToolTip.Tip="Разложить граф строками с переносом от стартовой ноды"
                        Click="OnAutoLayoutClicked" />
                <Button Grid.Column="10"
                        Content="Сохранить"
                        Classes="accent"
                        Height="26"
                        Padding="16,0"
                        Margin="6,0,0,0"
                        IsEnabled="{Binding HasOpenMacro}"
                        Click="OnSaveClicked" />
            </Grid>
        </Border>

        <Grid Grid.Row="1" ColumnDefinitions="228,*,308">

            <!-- =============================== library =============================== -->
            <Border Grid.Column="0"
                    Background="{DynamicResource NocturneSurfaceSunkenBrush}"
                    BorderBrush="{DynamicResource NocturneDividerBrush}"
                    BorderThickness="0,0,1,0">
                <Grid RowDefinitions="Auto,*,Auto">
                    <TextBox Grid.Row="0"
                             Margin="8"
                             Height="24"
                             Watermark="поиск…"
                             Text="{Binding LibrarySearch, Mode=TwoWay}" />

                    <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Disabled">
                        <ItemsControl ItemsSource="{Binding MacroGroups}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="canvas:MacroLibraryGroupViewModel">
                                    <StackPanel>
                                        <TextBlock Classes="eyebrow"
                                                   Margin="10,8,10,4"
                                                   Text="{Binding Header}" />
                                        <ItemsControl ItemsSource="{Binding Items}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate x:DataType="vm:MacroListItemViewModel">
                                                    <Border Classes="libRow"
                                                            Classes.current="{Binding IsCurrent}"
                                                            ToolTip.Tip="{Binding Summary}"
                                                            PointerPressed="OnMacroRowPressed">
                                                        <Grid ColumnDefinitions="*,Auto">
                                                            <StackPanel Grid.Column="0"
                                                                        Orientation="Horizontal"
                                                                        Spacing="7"
                                                                        VerticalAlignment="Center">
                                                                <TextBlock FontSize="11.5"
                                                                           VerticalAlignment="Center"
                                                                           TextTrimming="CharacterEllipsis"
                                                                           Foreground="{Binding IsCurrent, Converter={x:Static conv:TokenBrushConverter.Instance}, ConverterParameter=NocturneTextBrush|NocturneTextSecondaryBrush}"
                                                                           Text="{Binding Name}" />
                                                                <Border Padding="5,1"
                                                                        CornerRadius="3"
                                                                        VerticalAlignment="Center"
                                                                        Background="{DynamicResource NocturneSurfaceInsetBrush}"
                                                                        IsVisible="{Binding HasTriggerBadge}">
                                                                    <TextBlock FontSize="9.5"
                                                                               FontFamily="{DynamicResource NocturneFontFamilyMono}"
                                                                               Foreground="{DynamicResource NocturneTextMutedBrush}"
                                                                               Text="{Binding TriggerBadge}" />
                                                                </Border>
                                                                <!-- The chord is bound but Windows would not give it to
                                                                     us. Shown on the ROW because that is the only place
                                                                     it can be noticed without opening the macro — and
                                                                     the usual way this happens is at daemon startup,
                                                                     to a macro nobody is editing. -->
                                                                <TextBlock FontSize="9"
                                                                           VerticalAlignment="Center"
                                                                           Foreground="{DynamicResource NocturneDangerBrush}"
                                                                           IsVisible="{Binding HasHotkeyProblem}"
                                                                           ToolTip.Tip="{Binding HotkeyProblem}"
                                                                           Text="&#x25B2;" />
                                                            </StackPanel>
                                                            <StackPanel Grid.Column="1"
                                                                        Orientation="Horizontal"
                                                                        VerticalAlignment="Center">
                                                                <Button Content="▸"
                                                                        Classes="ghost"
                                                                        Padding="4,0"
                                                                        FontSize="10"
                                                                        ToolTip.Tip="Запустить"
                                                                        IsEnabled="{Binding IsNotRunning}"
                                                                        Click="OnRunMacroClicked" />
                                                                <Button Content="■"
                                                                        Classes="ghost"
                                                                        Padding="4,0"
                                                                        FontSize="9"
                                                                        ToolTip.Tip="Остановить"
                                                                        IsEnabled="{Binding IsRunning}"
                                                                        Click="OnStopMacroClicked" />
                                                                <Button Content="×"
                                                                        Classes="ghost"
                                                                        Padding="4,0"
                                                                        FontSize="11"
                                                                        ToolTip.Tip="Удалить"
                                                                        Click="OnDeleteMacroClicked" />
                                                            </StackPanel>
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <Border Grid.Row="2"
                            Padding="8"
                            BorderThickness="0,1,0,0"
                            BorderBrush="{DynamicResource NocturneDividerBrush}">
                        <Button Content="+ Новый макрос"
                                Classes="accent"
                                Height="26"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Center"
                                Click="OnNewMacroClicked" />
                    </Border>
                </Grid>
            </Border>

            <!-- ========================= canvas + run log ========================= -->
            <Grid Grid.Column="1" RowDefinitions="*,158">

                <Panel Grid.Row="0"
                       x:Name="Viewport"
                       Background="{DynamicResource NocturneCanvasBrush}"
                       ClipToBounds="True"
                       Focusable="True"
                       PointerPressed="OnViewportPressed"
                       PointerMoved="OnViewportMoved"
                       PointerReleased="OnViewportReleased"
                       PointerWheelChanged="OnViewportWheel"
                       KeyDown="OnViewportKeyDown">

                    <controls:CanvasBackdrop Zoom="{Binding Zoom}"
                                             OffsetX="{Binding PanX}"
                                             OffsetY="{Binding PanY}" />

                    <!-- The transformed surface. Everything on it is in canvas coordinates;
                         the code-behind converts pointer positions with the same numbers. -->
                    <Canvas x:Name="Surface"
                            Width="4000"
                            Height="4000"
                            Background="Transparent"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Top"
                            RenderTransformOrigin="0%,0%"
                            IsVisible="{Binding HasOpenMacro}">
                        <controls:EdgeLayer x:Name="Edges"
                                            Width="4000"
                                            Height="4000"
                                            Edges="{Binding CanvasEdges}"
                                            Links="{Binding VariableLinks}" />
                        <ItemsControl ItemsSource="{Binding Nodes}"
                                      ItemTemplate="{StaticResource NodeBoxTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Canvas Width="4000" Height="4000" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.Styles>
                                <!-- The generated container is what carries the position;
                                     the box template itself knows nothing about where it is.
                                     ClipToBounds is turned OFF explicitly: the breakpoint dot
                                     hangs off the box's top-left corner on a negative margin,
                                     and a node at canvas x=0 had its left half sliced away. -->
                                <Style Selector="ItemsControl > ContentPresenter" x:DataType="nodes:NodeRowViewModel">
                                    <Setter Property="Canvas.Left" Value="{Binding X}" />
                                    <Setter Property="Canvas.Top" Value="{Binding Y}" />
                                    <Setter Property="ClipToBounds" Value="False" />
                                </Style>
                            </ItemsControl.Styles>
                        </ItemsControl>
                    </Canvas>

                    <!-- empty state -->
                    <StackPanel VerticalAlignment="Center"
                                HorizontalAlignment="Center"
                                Spacing="8.4"
                                IsVisible="{Binding !HasOpenMacro}">
                        <TextBlock FontSize="{DynamicResource NocturneFontSizeTitle}"
                                   HorizontalAlignment="Center"
                                   Foreground="{DynamicResource NocturneTextMutedBrush}"
                                   Text="Выберите макрос слева или создайте новый." />
                        <TextBlock FontSize="{DynamicResource NocturneFontSizeSmall}"
                                   FontFamily="{DynamicResource NocturneFontFamilyMono}"
                                   Foreground="{DynamicResource NocturneTextGhostBrush}"
                                   HorizontalAlignment="Center"
                                   TextWrapping="Wrap"
                                   Text="{Binding FolderPath}" />
                    </StackPanel>

                    <!-- Zoom chip. Bottom-LEFT, unlike the mockup's top-right: the mockup's
                         canvas is wider than its graph, ours is exactly as wide, so a
                         top-right chip sits on top of the third column's first box. -->
                    <Border HorizontalAlignment="Left"
                            VerticalAlignment="Bottom"
                            Margin="12"
                            Height="24"
                            Padding="4,0"
                            CornerRadius="{DynamicResource NocturneRadiusSm}"
                            Background="{DynamicResource NocturneSurfaceOverlayBrush}"
                            BorderBrush="{DynamicResource NocturneDividerStrongBrush}"
                            BorderThickness="1"
                            IsVisible="{Binding HasOpenMacro}">
                        <StackPanel Orientation="Horizontal" Spacing="2" VerticalAlignment="Center">
                            <Button Content="−" Classes="ghost" Padding="6,0" ToolTip.Tip="Мельче" Click="OnZoomOutClicked" />
                            <Button Classes="ghost" Padding="4,0" ToolTip.Tip="Сбросить масштаб" Click="OnZoomResetClicked">
                                <TextBlock FontSize="10.5"
                                           FontFamily="{DynamicResource NocturneFontFamilyMono}"
                                           MinWidth="34"
                                           TextAlignment="Center"
                                           Text="{Binding ZoomText}" />
                            </Button>
                            <Button Content="+" Classes="ghost" Padding="6,0" ToolTip.Tip="Крупнее" Click="OnZoomInClicked" />
                        </StackPanel>
                    </Border>

                    <!-- legend -->
                    <Border HorizontalAlignment="Right"
                            VerticalAlignment="Bottom"
                            Margin="12"
                            Height="24"
                            Padding="10,0"
                            CornerRadius="{DynamicResource NocturneRadiusSm}"
                            Background="{DynamicResource NocturneSurfaceOverlayBrush}"
                            BorderBrush="{DynamicResource NocturneDividerStrongBrush}"
                            BorderThickness="1"
                            IsVisible="{Binding HasOpenMacro}">
                        <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal" Spacing="5">
                                <Border Width="7" Height="7"
                                        VerticalAlignment="Center"
                                        RenderTransform="rotate(45deg)"
                                        BorderThickness="1"
                                        BorderBrush="{DynamicResource NocturneAccentBrush}" />
                                <TextBlock Classes="hint" Text="условие" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Spacing="5">
                                <Border Width="7" Height="7"
                                        CornerRadius="2"
                                        VerticalAlignment="Center"
                                        Background="{DynamicResource NocturneTextFaintBrush}" />
                                <TextBlock Classes="hint" Text="действие" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Spacing="5">
                                <Border Width="12" Height="1"
                                        VerticalAlignment="Center"
                                        Background="{DynamicResource NocturneVariableBrush}" />
                                <TextBlock Classes="hint" Text="переменная" />
                            </StackPanel>
                            <TextBlock Classes="hint" Text="СКМ или пустое место — панорама · колесо — зум" />
                        </StackPanel>
                    </Border>
                </Panel>

                <!-- ============================= run log ============================= -->
                <Border Grid.Row="1"
                        Background="{DynamicResource NocturnePanelDeepBrush}"
                        BorderBrush="{DynamicResource NocturneDividerBrush}"
                        BorderThickness="0,1,0,0">
                    <Grid RowDefinitions="26,*">
                        <Border Grid.Row="0"
                                Padding="12,0"
                                BorderThickness="0,0,0,1"
                                BorderBrush="{DynamicResource NocturnePanelDeepDividerBrush}">
                            <Grid ColumnDefinitions="Auto,Auto,Auto,*,Auto">
                                <TextBlock Grid.Column="0"
                                           FontSize="10.5"
                                           FontWeight="Medium"
                                           VerticalAlignment="Center"
                                           Foreground="{DynamicResource NocturneNeutral300Brush}"
                                           Text="Лог прогона" />

                                <!-- The run picker. One graph can be walked by ten windows at
                                     once, so the strip and the canvas highlight follow ONE of
                                     them and this is how it is chosen. -->
                                <StackPanel Grid.Column="1"
                                            Orientation="Horizontal"
                                            Spacing="6"
                                            Margin="10,0,0,0"
                                            VerticalAlignment="Center"
                                            IsVisible="{Binding HasRuns}">
                                    <Ellipse Width="5" Height="5"
                                             VerticalAlignment="Center"
                                             IsVisible="{Binding SelectedRunIsLive}"
                                             Fill="{DynamicResource NocturneAccentBrush}" />
                                    <TextBlock FontSize="10"
                                               VerticalAlignment="Center"
                                               FontFamily="{DynamicResource NocturneFontFamilyMono}"
                                               Foreground="{DynamicResource NocturneTextMutedBrush}"
                                               Text="{Binding RunChipText}" />
                                    <TextBlock Classes="hint"
                                               Text="{Binding RunPositionText}" />
                                    <!-- U+25C2/U+25B8, not U+25C0/U+25B6: D1 found the large
                                         triangles get emoji presentation and arrive white,
                                         ignoring Foreground entirely. -->
                                    <Button Classes="ghost" Padding="5,0" FontSize="11"
                                            Content="&#x25C2;"
                                            ToolTip.Tip="Предыдущий прогон"
                                            Click="OnPreviousRunClicked" />
                                    <Button Classes="ghost" Padding="5,0" FontSize="11"
                                            Content="&#x25B8;"
                                            ToolTip.Tip="Следующий прогон"
                                            Click="OnNextRunClicked" />
                                </StackPanel>

                                <!-- Never let a partial log look complete. -->
                                <Border Grid.Column="2"
                                        Margin="10,0,0,0"
                                        Padding="7,1"
                                        VerticalAlignment="Center"
                                        CornerRadius="9"
                                        Background="{DynamicResource NocturneWarningSurfaceBrush}"
                                        BorderBrush="{DynamicResource NocturneWarningBorderBrush}"
                                        BorderThickness="1"
                                        IsVisible="{Binding HasRunLogNotice}">
                                    <TextBlock FontSize="9.5"
                                               Foreground="{DynamicResource NocturneWarningBrush}"
                                               Text="{Binding RunLogNotice}" />
                                </Border>

                                <!-- Mockup 1d's red pill, widened to every pause reason: this
                                     is the ONE place that names both the state and the node
                                     it is parked at, so it has to cover a step and a manual
                                     pause too, not only a breakpoint. Danger tint only for a
                                     breakpoint — the others are the user's own doing. -->
                                <Border Grid.Column="3"
                                        Margin="10,0,0,0"
                                        Padding="7,1"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center"
                                        CornerRadius="9"
                                        Background="{Binding SelectedRunAtBreakpoint, Converter={x:Static conv:TokenBrushConverter.Instance}, ConverterParameter=NocturneDangerSurfaceBrush|NocturneAccentTint12Brush}"
                                        BorderBrush="{Binding SelectedRunAtBreakpoint, Converter={x:Static conv:TokenBrushConverter.Instance}, ConverterParameter=NocturneDangerBorderBrush|NocturneAccent700Brush}"
                                        BorderThickness="1"
                                        IsVisible="{Binding SelectedRunPauseVisible}">
                                    <StackPanel Orientation="Horizontal" Spacing="5">
                                        <Ellipse Width="5" Height="5"
                                                 VerticalAlignment="Center"
                                                 Fill="{Binding SelectedRunAtBreakpoint, Converter={x:Static conv:TokenBrushConverter.Instance}, ConverterParameter=NocturneDangerBrush|NocturneAccentBrush}" />
                                        <TextBlock FontSize="9.5"
                                                   Foreground="{Binding SelectedRunAtBreakpoint, Converter={x:Static conv:TokenBrushConverter.Instance}, ConverterParameter=NocturneDangerBrush|NocturneAccent300Brush}"
                                                   Text="{Binding PauseNotice}" />
                                    </StackPanel>
                                </Border>

                                <TextBlock Grid.Column="3"
                                           Classes="hint"
                                           Margin="10,0,0,0"
                                           TextTrimming="CharacterEllipsis"
                                           IsVisible="{Binding !SelectedRunPauseVisible}"
                                           Text="нода · исход · деталь · длительность" />

                                <Button Grid.Column="4"
                                        Classes="ghost"
                                        Padding="6,0"
                                        FontSize="10"
                                        Content="очистить"
                                        ToolTip.Tip="Забыть записанные прогоны"
                                        IsVisible="{Binding HasRuns}"
                                        Click="OnClearRunLogClicked" />
                            </Grid>
                        </Border>

                        <TextBlock Grid.Row="1"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontSize="{DynamicResource NocturneFontSizeSmall}"
                                   Foreground="{DynamicResource NocturneTextGhostBrush}"
                                   IsVisible="{Binding !HasRunLog}"
                                   Text="{Binding RunLogEmptyText}" />

                        <ScrollViewer Grid.Row="1" IsVisible="{Binding HasRunLog}">
                            <ItemsControl ItemsSource="{Binding RunLog}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="canvas:RunLogRowViewModel">
                                        <Border Padding="12,3"
                                                BorderThickness="2,0,0,0"
                                                BorderBrush="{Binding IsCurrent, Converter={x:Static conv:TokenBrushConverter.Instance}, ConverterParameter=NocturneAccentBrush|NocturneCanvasBrush}"
                                                Background="{Binding IsCurrent, Converter={x:Static conv:TokenBrushConverter.Instance}, ConverterParameter=NocturneAccentTint08Brush|NocturnePanelDeepBrush}">
                                            <Grid ColumnDefinitions="62,156,96,*">
                                                <TextBlock Grid.Column="0"
                                                           FontSize="10.5"
                                                           FontFamily="{DynamicResource NocturneFontFamilyMono}"
                                                           Foreground="{DynamicResource NocturneTextMutedBrush}"
                                                           Text="{Binding Elapsed}" />
                                                <TextBlock Grid.Column="1"
                                                           FontSize="10.5"
                                                           FontFamily="{DynamicResource NocturneFontFamilyMono}"
                                                           TextTrimming="CharacterEllipsis"
                                                           Foreground="{DynamicResource NocturneTextMutedBrush}"
                                                           Text="{Binding NodeId}" />
                                                <TextBlock Grid.Column="2"
                                                           FontSize="10.5"
                                                           FontFamily="{DynamicResource NocturneFontFamilyMono}"
                                                           Foreground="{Binding OutcomeIsAccent, Converter={x:Static conv:TokenBrushConverter.Instance}, ConverterParameter=NocturneAccent400Brush|NocturneTextMutedBrush}"
                                                           Text="{Binding Outcome}" />
                                                <TextBlock Grid.Column="3"
                                                           FontSize="10.5"
                                                           FontFamily="{DynamicResource NocturneFontFamilyMono}"
                                                           TextTrimming="CharacterEllipsis"
                                                           Foreground="{DynamicResource NocturneTextFaintBrush}"
                                                           Text="{Binding Detail}" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>

            <!-- ============================== inspector ============================== -->
            <Border Grid.Column="2"
                    Background="{DynamicResource NocturnePanelDeepBrush}"
                    BorderBrush="{DynamicResource NocturneDividerBrush}"
                    BorderThickness="1,0,0,0">
                <Grid RowDefinitions="30,*,Auto">

                    <Border Grid.Row="0"
                            Padding="12,0"
                            Background="{DynamicResource NocturneSurfaceRaisedBrush}"
                            BorderThickness="0,0,0,1"
                            BorderBrush="{DynamicResource NocturnePanelDeepDividerBrush}">
                        <Grid ColumnDefinitions="Auto,*,Auto">
                            <TextBlock Grid.Column="0"
                                       FontSize="11.5"
                                       FontWeight="Medium"
                                       VerticalAlignment="Center"
                                       Text="{Binding InspectorTitle}" />
                            <TextBlock Grid.Column="2"
                                       Classes="hint"
                                       IsVisible="{Binding HasSelectedNode}"
                                       Text="двойной клик — правка на месте" />
                        </Grid>
                    </Border>

                    <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel>

                            <!-- node inspector -->
                            <StackPanel IsVisible="{Binding HasSelectedNode}">
                                <StackPanel Margin="12,9" Spacing="5.6">
                                    <Grid ColumnDefinitions="64,*">
                                        <TextBlock Grid.Column="0" Classes="label" Text="id" />
                                        <TextBox Grid.Column="1"
                                                 Classes="mono"
                                                 ToolTip.Tip="Переименование автоматически перешивает входящие рёбра"
                                                 Text="{Binding SelectedNode.NodeId, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                                    </Grid>
                                    <ContentControl Content="{Binding SelectedNode}" />
                                </StackPanel>

                                <Border BorderThickness="0,1,0,0"
                                        BorderBrush="{DynamicResource NocturnePanelDeepDividerBrush}"
                                        Padding="12,9"
                                        IsVisible="{Binding SelectedNode.HasTarget}">
                                    <StackPanel Spacing="5.6">
                                        <Grid ColumnDefinitions="Auto,*">
                                            <TextBlock Grid.Column="0" Classes="eyebrow" Text="ЦЕЛИ" />
                                            <!-- The badge with its expansion (1g). Here rather
                                                 than on the canvas box because a Popup over a
                                                 scaled+translated surface lands where the
                                                 transform is not. -->
                                            <ContentControl Grid.Column="1"
                                                            HorizontalAlignment="Right"
                                                            Content="{Binding SelectedNode.Target}"
                                                            ContentTemplate="{StaticResource TargetBadgeButtonTemplate}" />
                                        </Grid>
                                        <ContentControl Content="{Binding SelectedNode.Target}" />
                                    </StackPanel>
                                </Border>

                                <Border BorderThickness="0,1,0,0"
                                        BorderBrush="{DynamicResource NocturnePanelDeepDividerBrush}"
                                        Padding="12,9">
                                    <StackPanel Spacing="5.6">
                                        <TextBlock Classes="eyebrow" Text="ВЫХОДЫ" />
                                        <ItemsControl ItemsSource="{Binding SelectedNode.Edges}"
                                                      ItemTemplate="{StaticResource EdgeEditorTemplate}" />
                                        <TextBlock Classes="hint"
                                                   TextWrapping="Wrap"
                                                   Text="«— конец —» — прогон заканчивается на этом исходе. Это не ошибка и не отдельная нода." />
                                        <!-- Mockup 1d puts the breakpoint checkbox here, at
                                             the foot of the outputs block. It is NOT part of
                                             the graph: it goes to the daemon's session and
                                             never into the macro file. -->
                                        <CheckBox Margin="0,4,0,0"
                                                  Content="Брейкпоинт на этой ноде"
                                                  ToolTip.Tip="Прогон встанет ПЕРЕД этой нодой. Хранится в демоне, не в файле макроса."
                                                  IsChecked="{Binding SelectedNode.HasBreakpoint, Mode=TwoWay}" />
                                    </StackPanel>
                                </Border>

                                <Border BorderThickness="0,1,0,0"
                                        BorderBrush="{DynamicResource NocturnePanelDeepDividerBrush}"
                                        Padding="12,9">
                                    <Button Content="Удалить ноду"
                                            Classes="danger"
                                            HorizontalAlignment="Stretch"
                                            HorizontalContentAlignment="Center"
                                            Click="OnDeleteNodeClicked" />
                                </Border>
                            </StackPanel>

                            <!-- macro inspector (nothing selected) -->
                            <StackPanel Margin="12,9"
                                        Spacing="8.4"
                                        IsVisible="{Binding !HasSelectedNode}">
                                <TextBlock Classes="hint"
                                           TextWrapping="Wrap"
                                           IsVisible="{Binding HasOpenMacro}"
                                           Text="Кликните бокс, чтобы править его здесь. Двойной клик разворачивает бокс на месте." />
                                <TextBlock Classes="hint"
                                           TextWrapping="Wrap"
                                           IsVisible="{Binding !HasOpenMacro}"
                                           Text="Макрос не открыт." />
                            </StackPanel>

                            <Border Padding="12,9"
                                    BorderThickness="0,1,0,0"
                                    BorderBrush="{DynamicResource NocturnePanelDeepDividerBrush}"
                                    IsVisible="{Binding HasOpenMacro}">
                                <StackPanel Spacing="5.6">
                                    <Grid ColumnDefinitions="*,Auto,Auto">
                                        <TextBlock Grid.Column="0" Classes="eyebrow" Text="ТРИГГЕРЫ" />
                                        <Button Grid.Column="1"
                                                Content="+ хоткей"
                                                Classes="ghost"
                                                Click="OnAddHotkeyTriggerClicked" />
                                        <Button Grid.Column="2"
                                                Content="+ процесс"
                                                Classes="ghost"
                                                Click="OnAddProcessTriggerClicked" />
                                    </Grid>
                                    <ItemsControl ItemsSource="{Binding Triggers}" />
                                    <!-- The standing "hotkeys are off in this mode" line
                                         used to live here. D4 moved it into the picker's
                                         armed state (1f), which is the moment it is
                                         actionable; two copies of the same sentence, one of
                                         them permanent, is how a warning stops being read. -->
                                </StackPanel>
                            </Border>

                            <!-- ==== variables (D5) ====
                                 Structure from the static analysis in Contracts, value from
                                 the selected walk. Shown whatever the inspector's other half
                                 is doing, like validation: it is about the MACRO, and hiding
                                 it behind a node selection would make «кто пишет {tag}»
                                 answerable only from the node that happens to be selected. -->
                            <Border Padding="12,9"
                                    BorderThickness="0,1,0,0"
                                    BorderBrush="{DynamicResource NocturnePanelDeepDividerBrush}"
                                    Background="{DynamicResource NocturneCanvasBrush}"
                                    IsVisible="{Binding HasOpenMacro}">
                                <StackPanel Spacing="8.4">
                                    <Grid ColumnDefinitions="*,Auto">
                                        <TextBlock Grid.Column="0" Classes="eyebrow" Text="ПЕРЕМЕННЫЕ МАКРОСА" />
                                        <TextBlock Grid.Column="1" Classes="hint" Text="{Binding VariableCountText}" />
                                    </Grid>

                                    <ItemsControl ItemsSource="{Binding Variables}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate x:DataType="canvas:MacroVariableRowViewModel">
                                                <Border Margin="0,0,0,5.6"
                                                        CornerRadius="6"
                                                        BorderThickness="1"
                                                        Background="{DynamicResource NocturneSurfaceRowAltBrush}"
                                                        BorderBrush="{Binding IsHighlighted, Converter={x:Static conv:TokenBrushConverter.Instance}, ConverterParameter=NocturneVariableBrush|NocturneDividerStrongBrush}"
                                                        PointerEntered="OnVariableHovered"
                                                        PointerExited="OnVariableUnhovered">
                                                    <StackPanel>
                                                        <Border Padding="8,5"
                                                                BorderThickness="0,0,0,1"
                                                                BorderBrush="{DynamicResource NocturneDividerStrongBrush}"
                                                                Background="{DynamicResource NocturneVariableSurfaceBrush}">
                                                            <Grid ColumnDefinitions="Auto,Auto,*,Auto">
                                                                <TextBlock Grid.Column="0"
                                                                           FontSize="11"
                                                                           FontWeight="Medium"
                                                                           FontFamily="{DynamicResource NocturneFontFamilyMono}"
                                                                           Foreground="{DynamicResource NocturneVariableBrush}"
                                                                           Text="{Binding Name}" />
                                                                <TextBlock Grid.Column="1"
                                                                           Margin="7,0,0,0"
                                                                           Classes="hint"
                                                                           Text="{Binding KindText}" />
                                                                <!-- Nothing rather than a dash when the walk
                                                                     has not set it: an empty cell reads as
                                                                     "not yet", a dash reads as "empty
                                                                     string", and those are different bugs. -->
                                                                <TextBlock Grid.Column="3"
                                                                           FontSize="10"
                                                                           FontFamily="{DynamicResource NocturneFontFamilyMono}"
                                                                           TextTrimming="CharacterEllipsis"
                                                                           IsVisible="{Binding HasValue}"
                                                                           Text="{Binding Value}" />
                                                            </Grid>
                                                        </Border>
                                                        <Grid ColumnDefinitions="46,*" Margin="8,4,8,0">
                                                            <TextBlock Grid.Column="0" Classes="hint" Text="пишет" />
                                                            <TextBlock Grid.Column="1"
                                                                       FontSize="10.5"
                                                                       FontFamily="{DynamicResource NocturneFontFamilyMono}"
                                                                       TextTrimming="CharacterEllipsis"
                                                                       Foreground="{DynamicResource NocturneNeutral300Brush}"
                                                                       Text="{Binding WrittenBy}" />
                                                        </Grid>
                                                        <Grid ColumnDefinitions="46,*" Margin="8,1,8,5"
                                                              ToolTip.Tip="{Binding ReadDetail}">
                                                            <TextBlock Grid.Column="0" Classes="hint" Text="читает" />
                                                            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6">
                                                                <TextBlock FontSize="10.5"
                                                                           FontFamily="{DynamicResource NocturneFontFamilyMono}"
                                                                           TextTrimming="CharacterEllipsis"
                                                                           Foreground="{DynamicResource NocturneNeutral300Brush}"
                                                                           Text="{Binding ReadBy}" />
                                                                <TextBlock Classes="hint" Text="{Binding ReadWhere}" />
                                                            </StackPanel>
                                                        </Grid>
                                                        <!-- A read with no writer aborts the run at that
                                                             node (spec §5.3). Saying so here is cheaper
                                                             than finding out from a log line. -->
                                                        <TextBlock Margin="8,0,8,5"
                                                                   FontSize="9.5"
                                                                   TextWrapping="Wrap"
                                                                   IsVisible="{Binding IsUndefined}"
                                                                   Foreground="{DynamicResource NocturneDangerBrush}"
                                                                   Text="никто не пишет — прогон прервётся на первом чтении" />
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                    <TextBlock Classes="hint"
                                               TextWrapping="Wrap"
                                               Text="Наведение подсвечивает на canvas ноду-источник и потребителей и рисует между ними пунктир." />
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Validation stays visible whatever the inspector is showing: it is
                         about the graph, and hiding it behind a selection is how a blocked
                         save becomes a mystery. -->
                    <Border Grid.Row="2"
                            Margin="8"
                            Padding="{DynamicResource NocturneInsetTight}"
                            BorderThickness="1"
                            BorderBrush="{DynamicResource NocturneDangerBorderBrush}"
                            Background="{DynamicResource NocturneDangerSurfaceBrush}"
                            CornerRadius="{DynamicResource NocturneRadiusSm}"
                            IsVisible="{Binding HasIssues}">
                        <ScrollViewer MaxHeight="140">
                            <ItemsControl ItemsSource="{Binding Issues}" />
                        </ScrollViewer>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
