using PerfectWorldAgent.Models;

namespace PerfectWorldAgent.Vision;

public sealed class CoordinateReader
{
    public Coordinates Read(byte[] screenshot) =>
        throw new NotImplementedException("Tesseract OCR on coordinate UI region");
}
